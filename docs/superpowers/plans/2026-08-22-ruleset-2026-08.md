# 룰셋 2026-08 반영 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 현행 고시와 어긋난 룰셋 값 넷을 바로잡고, `absoluteCap`을 주택가격 구간 함수로 바꿔 15억 초과 주택의 한도 과대계상을 없앤다.

**Architecture:** `absoluteCap`을 스칼라에서 구간 배열로 바꾸는 스키마 변경이 중심이다. 이 변경이 세 곳으로 번진다 — `calcMaxLoan`이 가격에 따라 캡을 정하고, `buildSearchSegments`가 새 절벽에서 탐색을 나누고, `parseRules`의 정책대출 불변식이 구간 캡과 비교한다. 기존 `rules/2026-03.json`은 값을 그대로 둔 채 새 스키마로 옮겨, 그 파일을 쓰는 기존 테스트 전부가 마이그레이션 등가성 검증 장치가 된다.

**Tech Stack:** TypeScript strict + `noUncheckedIndexedAccess`, Vitest 4, React 19 + Vite 7

## Global Constraints

- **모든 금액은 원 단위 정수로 다룬다.** 비율은 소수(0.4 = 40%).
- **`absoluteCap` 구간 조회는 상한 포함(`price <= upTo`)이다.** 이 파일의 다른 구간 배열(`housingBond.brackets`, `brokerageFee`)과 취득세는 배타(`price < upTo`)다. 두 의미가 공존하므로 조회 함수 doc과 룰셋 `_note` 양쪽에 이유를 남긴다.
- **취득세의 배타적 경계는 이번에 건드리지 않는다.** 안전한 방향의 오차이며 범위 밖이다.
- **디딤돌 자격별 한도 축(생애최초 3억 / 신혼 4억)은 이번에 넣지 않는다.** 현재 2.5억 단일은 과소 계상이라 안전한 방향이다.
- **`rules/2026-03.json`의 값은 하나도 바뀌지 않는다.** `absoluteCap`의 표현 형식만 바뀐다.
- **기존 테스트는 수정 대상이 아니다.** 단 하나의 예외: `src/state/useAffordability.test.ts`의 버전 문자열 `"2026-03"` → `"2026-08"`. 다른 테스트가 깨지면 기대값을 고치지 말고 마이그레이션을 의심한다.
- **정책대출 금리는 고시 범위의 위쪽 값을 쓴다.** 우대금리는 사람마다 다르고, 높은 금리는 한도를 낮춰 안전한 방향으로 틀린다.
- 파일당 하나의 책임. 파일을 쪼개지 말고 같은 파일 안에서 헬퍼로 추출한다.
- `any` 금지, 불필요한 non-null assertion 금지.
- 테스트는 대상과 같은 디렉토리에 `*.test.ts`로 둔다.
- 커밋 메시지는 Conventional Commits, 제목은 한국어.
- **`src/no-network.test.ts`가 감시하는 네트워크 금지 약속을 깨지 않는다.** 이 작업은 네트워크 코드를 추가하지 않는다.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `src/lib/finance/types.ts` | `Rules.absoluteCap` 타입 | 스칼라 → 구간 배열 |
| `src/lib/finance/rules.ts` | 파싱·검증 | `absoluteCap` 구조 검증, 정책대출 불변식 |
| `src/lib/finance/loan-limit.ts` | 대출 한도 계산 | `calcAbsoluteCap` 신설, `calcMaxLoan`이 사용 |
| `src/lib/finance/affordable-price.ts` | 감당 가능 최대가 탐색 | `buildSearchSegments`에 캡 경계 추가 |
| `rules/2026-03.json` | 옛 고시값 보존 | `absoluteCap` 형식만 마이그레이션 |
| `rules/2026-08.json` | 현행 고시값 | 신규 |
| `src/state/useAffordability.ts` | 화면이 쓰는 룰셋 | import 교체 |
| `src/lib/finance/golden-2026-08.test.ts` | 2026-08 공식 수치 대조 | 신규 |

---

## Task 1: `absoluteCap` 스키마와 조회

`absoluteCap`을 구간 배열로 바꾸고, 가격을 받아 캡을 돌려주는 조회 함수를 만든다. 이 태스크가 끝나면 기존 테스트가 전부 통과하는 상태여야 한다(마이그레이션 등가성).

**Files:**
- Modify: `src/lib/finance/types.ts` (`absoluteCap` 필드)
- Modify: `src/lib/finance/rules.ts` (필수 필드 목록, 검증, 불변식)
- Modify: `src/lib/finance/loan-limit.ts` (`calcAbsoluteCap` 신설, `calcMaxLoan`)
- Modify: `rules/2026-03.json`
- Test: `src/lib/finance/loan-limit.test.ts`, `src/lib/finance/rules.test.ts`

**Interfaces:**
- Produces: `calcAbsoluteCap(rules: Rules, price: number): number` — `loan-limit.ts`에서 export. Task 2가 쓴다.
- Produces: `Rules["absoluteCap"]` = `{ brackets: Array<{ upTo: number | null; amount: number }> }`

- [ ] **Step 1: 실패하는 테스트를 쓴다 — 캡 구간 경계**

`src/lib/finance/loan-limit.test.ts` 파일 끝에 추가한다. 상단 import에 `calcAbsoluteCap`을 더한다.

```ts
describe("calcAbsoluteCap — 주택가격 구간별 대출 절대한도", () => {
  const tiered = parseRules({
    ...rawRules,
    absoluteCap: {
      brackets: [
        { upTo: 1_500_000_000, amount: 600_000_000 },
        { upTo: 2_500_000_000, amount: 400_000_000 },
        { upTo: null, amount: 200_000_000 },
      ],
    },
  });

  // 규제 원문이 "15억 원 이하 → 6억"이므로 상한은 포함이다.
  // 배타로 읽으면 정확히 15억일 때 2억을 과소 계상한다.
  it.each([
    [1_499_999_999, 600_000_000],
    [1_500_000_000, 600_000_000],
    [1_500_000_001, 400_000_000],
    [2_499_999_999, 400_000_000],
    [2_500_000_000, 400_000_000],
    [2_500_000_001, 200_000_000],
  ])("가격 %d원에서 캡은 %d원이다", (price, expected) => {
    expect(calcAbsoluteCap(tiered, price)).toBe(expected);
  });

  it("가격 0원에서는 첫 구간의 캡이다", () => {
    expect(calcAbsoluteCap(tiered, 0)).toBe(600_000_000);
  });

  it("구간이 하나뿐이면 모든 가격에서 같은 캡이다", () => {
    const flat = parseRules({
      ...rawRules,
      absoluteCap: { brackets: [{ upTo: null, amount: 600_000_000 }] },
    });
    expect(calcAbsoluteCap(flat, 0)).toBe(600_000_000);
    expect(calcAbsoluteCap(flat, 10_000_000_000)).toBe(600_000_000);
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npx vitest run src/lib/finance/loan-limit.test.ts`
Expected: FAIL — `calcAbsoluteCap`이 export되지 않아 import 에러

- [ ] **Step 3: 타입을 바꾼다**

`src/lib/finance/types.ts`에서 이 줄을 찾는다:

```ts
  /** 수도권 주택구입 목적 주담대 절대 상한(원) */
  absoluteCap: number;
```

이렇게 바꾼다:

```ts
  /**
   * 수도권 주택구입 목적 주담대 절대 상한(원). 주택가격 구간별로 다르다.
   *
   * ⚠ **`upTo`는 포함(이하)이다.** 이 파일의 다른 구간 배열
   * (`housingBond.brackets`, `brokerageFee`)과 취득세 구간은 전부
   * 배타(미만)이므로 여기만 다르다. 규제 원문이 "15억 원 이하 → 6억"으로
   * 쓰기 때문이며, 배타로 읽으면 가격이 정확히 15억일 때 한도를 2억
   * 과소 계상한다. 조회는 반드시 `calcAbsoluteCap`을 쓴다.
   */
  absoluteCap: {
    /** upTo 오름차순, 마지막은 null(무한대). upTo는 포함 */
    brackets: Array<{ upTo: number | null; amount: number }>;
  };
```

- [ ] **Step 4: 파싱 검증을 바꾼다**

`src/lib/finance/rules.ts`에서 `REQUIRED_NUMBER_FIELDS` 배열의 `"absoluteCap",` 줄을 **삭제한다** (더 이상 숫자 필드가 아니다).

`housingBond.brackets` 검증 바로 아래(`validateAscendingBrackets(housingBond.brackets, ...)` 호출 다음, `const safetyThreshold = ...` 앞)에 다음을 추가한다:

```ts
  const absoluteCap = assertPlainObject(r.absoluteCap, "absoluteCap");
  if (!Array.isArray(absoluteCap.brackets) || absoluteCap.brackets.length === 0) {
    throw new Error("룰셋 필드 누락 또는 타입 오류: absoluteCap.brackets");
  }
  validateAscendingBrackets(
    absoluteCap.brackets,
    "absoluteCap.brackets",
    (obj, path) => assertNumberField(obj, "amount", `${path}.amount`),
  );
```

`validateSemanticInvariants` 안에서 이 줄을 찾아 **삭제한다**:

```ts
  assertNonNegative(rules.absoluteCap, "absoluteCap");
```

같은 함수 안, `rules.policyLoans.forEach(...)` 호출 **앞**에 다음을 추가한다:

```ts
  rules.absoluteCap.brackets.forEach((bracket, index) => {
    assertNonNegative(bracket.amount, `absoluteCap.brackets[${index}].amount`);
  });
```

- [ ] **Step 5: `calcAbsoluteCap`을 만들고 `calcMaxLoan`이 쓰게 한다**

`src/lib/finance/loan-limit.ts`에 다음 함수를 추가한다 (`calcMaxLoan` 정의 바로 앞):

```ts
/**
 * 주택가격에 해당하는 주담대 절대 상한(원).
 *
 * ⚠ **`upTo`는 포함(이하)이다.** 규제 원문이 "15억 원 이하 → 6억"으로
 * 쓰기 때문이다. 이 저장소의 다른 구간 조회(`acquisition-cost.ts`의
 * 취득세·채권 구간)는 전부 배타(`price < upTo`)이므로 여기만 다르다.
 * 배타로 통일하고 싶어지더라도 그러지 말 것 — 가격이 정확히 15억일 때
 * 한도를 6억이 아닌 4억으로 계산하게 되고, 15억은 실제로 나오는 호가다.
 *
 * 이 규칙은 여기 한 곳에만 있다. 룰셋 검증(`rules.ts`)도 이 함수를
 * import해서 쓴다 — 복제하면 검증과 계산이 조용히 어긋난다.
 */
export function calcAbsoluteCap(rules: Rules, price: number): number {
  for (const bracket of rules.absoluteCap.brackets) {
    if (bracket.upTo === null || price <= bracket.upTo) return bracket.amount;
  }
  // parseRules가 마지막 구간의 upTo === null을 강제하므로 여기 도달하지
  // 않는다. 그래도 조용히 undefined를 흘리지 않도록 끊는다.
  throw new Error("룰셋 값 오류: absoluteCap.brackets의 마지막 upTo가 null이 아닙니다");
}
```

같은 파일의 `calcMaxLoan` 안에서 이 줄을 찾는다:

```ts
    CAP: Math.floor(rules.absoluteCap),
```

이렇게 바꾼다:

```ts
    CAP: Math.floor(calcAbsoluteCap(rules, price)),
```

- [ ] **Step 6: 정책대출 불변식을 구간 캡에 맞게 바꾼다**

`src/lib/finance/rules.ts`의 `validateSemanticInvariants` 안에서 이 블록을 찾는다:

```ts
    // 정책대출 경로는 absoluteCap을 걸지 않는다(calcPolicyLimit 참고) —
    // 그 예외는 상품 고시 한도가 이미 지역 절대캡보다 한참 아래라는
    // 데이터 가정 위에 서 있다. 그 가정을 여기서 강제하지 않으면, 고시
    // 한도를 캡 이상으로 잘못 입력한 상품이 캡을 그대로 우회해 버린다.
    if (!(loan.maxAmount <= rules.absoluteCap)) {
      throw new Error(
        `룰셋 값 오류: ${path}.maxAmount는 absoluteCap(${rules.absoluteCap}) 이하여야 합니다 (${loan.maxAmount})`,
      );
    }
```

이렇게 바꾼다:

```ts
    // 정책대출 경로는 absoluteCap을 걸지 않는다(calcPolicyLimit 참고) —
    // 그 예외는 상품 고시 한도가 이미 지역 절대캡보다 한참 아래라는
    // 데이터 가정 위에 서 있다. 그 가정을 여기서 강제하지 않으면, 고시
    // 한도를 캡 이상으로 잘못 입력한 상품이 캡을 그대로 우회해 버린다.
    //
    // 캡이 주택가격 구간 함수가 되면서 "어느 구간의 캡과 비교할 것인가"가
    // 생겼다. 답은 **그 상품이 자격을 유지하는 최고 가격에서의 캡**이다.
    // 그보다 비싼 집에서는 애초에 그 상품을 받을 수 없으므로 우회가
    // 성립하지 않는다. 가격 상한이 없는 상품은 어떤 가격에서도 자격이
    // 있으므로 가장 낮은(=가장 엄격한) 구간의 캡과 비교한다.
    const capForLoan = capAtHighestEligiblePrice(rules, loan);
    if (!(loan.maxAmount <= capForLoan)) {
      throw new Error(
        `룰셋 값 오류: ${path}.maxAmount는 자격 최고가에서의 absoluteCap(${capForLoan}) 이하여야 합니다 (${loan.maxAmount})`,
      );
    }
```

같은 파일의 `validateAscendingBrackets` 함수 **바로 앞**에 헬퍼를 추가한다:

```ts
/**
 * 정책대출 상품 하나가 자격을 유지할 수 있는 최고 주택가격에서의 절대캡.
 *
 * `eligibility.maxHousePrice`가 있으면 그 가격에서의 캡이다. 없으면 어떤
 * 가격에서도 자격이 있다는 뜻이므로, 구간 중 가장 작은 캡을 쓴다 — 캡이
 * 가격에 따라 단조 감소한다는 보장이 스키마에 없으므로 최소값을 직접 구한다.
 *
 * 가격→캡 조회는 `loan-limit.ts`의 `calcAbsoluteCap` 하나만 쓴다. 검증과
 * 계산이 다른 규칙을 쓰면 조용히 어긋나므로 복제하지 않는다.
 */
function capAtHighestEligiblePrice(rules: Rules, loan: PolicyLoanRule): number {
  const maxPrice = loan.eligibility.maxHousePrice;
  if (maxPrice !== undefined) {
    return calcAbsoluteCap(rules, maxPrice);
  }
  let smallest = Number.POSITIVE_INFINITY;
  for (const bracket of rules.absoluteCap.brackets) {
    if (bracket.amount < smallest) smallest = bracket.amount;
  }
  return smallest;
}
```

`rules.ts` 상단에 `calcAbsoluteCap` import를 더한다. `PolicyLoanRule`은 이미 import돼 있다.

```ts
import { calcAbsoluteCap } from "./loan-limit";
import type { PolicyLoanRule, Rules } from "./types";
```

**순환 import는 생기지 않는다.** 확인된 의존 관계: `loan-limit.ts` → `amortization`(무의존), `policy-loans`(타입만), `profile`(타입만), `types`(무의존). `loan-limit.ts`는 `rules.ts`를 참조하지 않는다.

- [ ] **Step 7: `rules/2026-03.json`을 마이그레이션한다**

이 줄을 찾는다:

```json
  "absoluteCap": 600000000,
```

이렇게 바꾼다. **값의 의미는 100% 동일하다** — 모든 가격에서 6억이다.

```json
  "absoluteCap": {
    "_note": "2026-03 시점에는 주택가격과 무관하게 6억 단일이었다. 스키마가 구간 배열로 바뀌면서 형식만 옮긴 것이며, 값의 의미는 바뀌지 않았다. 이 파일을 쓰는 기존 테스트가 전부 그대로 통과하는 것이 그 증거다.",
    "brackets": [{ "upTo": null, "amount": 600000000 }]
  },
```

- [ ] **Step 8: 테스트가 통과하는지 확인한다**

Run: `npx vitest run src/lib/finance/`
Expected: PASS. 새 `calcAbsoluteCap` 테스트가 통과하고, **기존 테스트가 한 줄도 안 바뀐 채로 전부 통과**해야 한다.

기존 테스트가 하나라도 깨지면 **기대값을 고치지 말 것.** 마이그레이션이 값을 바꾼 것이므로 Step 7을 다시 본다.

- [ ] **Step 9: 불변식 테스트를 추가한다**

`src/lib/finance/rules.test.ts` 끝에 추가한다:

```ts
describe("정책대출 maxAmount와 구간별 absoluteCap 불변식", () => {
  const tieredCap = {
    brackets: [
      { upTo: 1_500_000_000, amount: 600_000_000 },
      { upTo: null, amount: 200_000_000 },
    ],
  };

  it("자격 최고가에서의 캡을 넘는 상품은 거부된다", () => {
    expect(() =>
      parseRules({
        ...rawRules,
        absoluteCap: tieredCap,
        policyLoans: [
          {
            id: "테스트",
            // 자격 최고가 20억 → 그 구간의 캡은 2억인데 한도가 3억이다
            eligibility: { maxHousePrice: 2_000_000_000 },
            maxAmount: 300_000_000,
            rate: 0.04,
          },
        ],
      }),
    ).toThrow(/absoluteCap/);
  });

  it("자격 최고가에서의 캡 이하인 상품은 통과한다", () => {
    expect(() =>
      parseRules({
        ...rawRules,
        absoluteCap: tieredCap,
        policyLoans: [
          {
            id: "테스트",
            // 자격 최고가 6억 → 그 구간의 캡은 6억이고 한도는 3.6억이다
            eligibility: { maxHousePrice: 600_000_000 },
            maxAmount: 360_000_000,
            rate: 0.04,
          },
        ],
      }),
    ).not.toThrow();
  });

  it("가격 상한이 없는 상품은 가장 낮은 구간의 캡과 비교된다", () => {
    expect(() =>
      parseRules({
        ...rawRules,
        absoluteCap: tieredCap,
        policyLoans: [
          {
            id: "테스트",
            eligibility: {},
            // 가장 낮은 구간 캡 2억을 넘는다
            maxAmount: 250_000_000,
            rate: 0.04,
          },
        ],
      }),
    ).toThrow(/absoluteCap/);
  });

  it("absoluteCap.brackets가 비어 있으면 거부된다", () => {
    expect(() =>
      parseRules({ ...rawRules, absoluteCap: { brackets: [] } }),
    ).toThrow(/absoluteCap.brackets/);
  });

  it("absoluteCap.brackets의 마지막 upTo가 null이 아니면 거부된다", () => {
    expect(() =>
      parseRules({
        ...rawRules,
        absoluteCap: { brackets: [{ upTo: 1_000_000_000, amount: 600_000_000 }] },
      }),
    ).toThrow(/upTo는 null/);
  });
});
```

- [ ] **Step 10: 전체 스위트와 타입체크를 돌린다**

Run: `npx vitest run && npx tsc --noEmit`
Expected: 전부 PASS, 타입 에러 없음

- [ ] **Step 11: 커밋한다**

```bash
git add src/lib/finance/types.ts src/lib/finance/rules.ts src/lib/finance/loan-limit.ts src/lib/finance/loan-limit.test.ts src/lib/finance/rules.test.ts rules/2026-03.json
git commit -m "feat: absoluteCap을 주택가격 구간 함수로 바꾼다

10·15 대책으로 수도권·규제지역 주담대 한도가 주택가격에 따라
6억/4억/2억으로 갈렸는데 스칼라 6억으로 남아 있어, 15억 초과 주택에서
한도를 최대 4억 과대 계상했다.

upTo를 포함(이하)으로 읽는다 — 이 저장소의 다른 구간 조회는 전부
배타(미만)이지만, 규제 원문이 '15억 원 이하'이고 배타로 읽으면 정확히
15억일 때 2억을 과소 계상하기 때문이다.

rules/2026-03.json은 값의 의미를 그대로 둔 채 형식만 옮겼다. 그 파일을
쓰는 기존 테스트가 한 줄도 안 바뀐 채 전부 통과하는 것이 등가성의 증거다."
```

---

## Task 2: 탐색 구간에 캡 절벽 추가

`calcAffordablePrice`의 이분 탐색이 캡 구간 경계를 가로지르지 않도록 구간을 나눈다.

**Files:**
- Modify: `src/lib/finance/affordable-price.ts` (`buildSearchSegments`)
- Test: `src/lib/finance/affordable-price.test.ts`

**Interfaces:**
- Consumes: Task 1의 `Rules["absoluteCap"]` 구조

- [ ] **Step 1: 실패하는 테스트를 쓴다 — 절벽을 넘나드는 탐색**

`src/lib/finance/affordable-price.test.ts` 끝에 추가한다.

```ts
describe("캡 구간 절벽을 넘나드는 탐색", () => {
  // 15억에서 캡이 6억 → 4억으로 떨어지는 룰셋
  const tiered = parseRules({
    ...rawRules,
    absoluteCap: {
      brackets: [
        { upTo: 1_500_000_000, amount: 600_000_000 },
        { upTo: 2_500_000_000, amount: 400_000_000 },
        { upTo: null, amount: 200_000_000 },
      ],
    },
  });

  // 절벽 근처에 답이 놓이도록 현금·소득을 크게 잡는다.
  const wealthy: BuyerProfile = {
    status: "무주택",
    cash: 1_100_000_000,
    annualIncome: 300_000_000,
    existingDebtAnnualPayment: 0,
    isFirstTimeBuyer: false,
    exclusiveAreaSqm: 84,
    isRegulatedArea: false,
  };

  /**
   * 이분 탐색이 절벽을 지나치지 않았는지 브루트포스로 확인한다.
   * PRICE_STEP 단위로 훑어 "현금으로 감당 가능한" 최대 가격을 직접 구하고,
   * 엔진 결과와 대조한다. 탐색 범위는 절벽 주변으로 좁혀 시간을 아낀다.
   */
  function bruteForceMax(
    profile: BuyerProfile,
    rules: ReturnType<typeof parseRules>,
    from: number,
    to: number,
  ): number {
    let best = 0;
    for (let price = from; price <= to; price += PRICE_STEP) {
      const loan = calcMaxLoan(profile, rules, price);
      const costs = calcAcquisitionCosts(profile, rules, price);
      const ownFunds = price - loan.amount + costs.total;
      if (ownFunds <= profile.cash) best = price;
    }
    return best;
  }

  it("엔진이 찾은 최대가가 브루트포스와 일치한다", () => {
    const result = calcAffordablePrice(wealthy, tiered);
    const brute = bruteForceMax(
      wealthy,
      tiered,
      1_400_000_000,
      1_700_000_000,
    );
    expect(result.affordablePrice).toBe(brute);
  });

  it("찾은 가격이 실제로 감당 가능하다", () => {
    const result = calcAffordablePrice(wealthy, tiered);
    const loan = calcMaxLoan(wealthy, tiered, result.affordablePrice);
    const costs = calcAcquisitionCosts(wealthy, tiered, result.affordablePrice);
    const ownFunds = result.affordablePrice - loan.amount + costs.total;
    expect(ownFunds).toBeLessThanOrEqual(wealthy.cash);
  });

  it("한 스텝 위는 감당 불가능하다 — 진짜 최대다", () => {
    const result = calcAffordablePrice(wealthy, tiered);
    const next = result.affordablePrice + PRICE_STEP;
    const loan = calcMaxLoan(wealthy, tiered, next);
    const costs = calcAcquisitionCosts(wealthy, tiered, next);
    const ownFunds = next - loan.amount + costs.total;
    expect(ownFunds).toBeGreaterThan(wealthy.cash);
  });
});
```

이 테스트가 쓰는 것들이 파일 상단에 import돼 있는지 확인하고, 없으면 더한다:

```ts
import { calcAcquisitionCosts } from "./acquisition-cost";
import { calcMaxLoan } from "./loan-limit";
import { calcAffordablePrice, PRICE_STEP } from "./affordable-price";
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npx vitest run src/lib/finance/affordable-price.test.ts`
Expected: FAIL — 탐색이 캡 절벽에서 나뉘지 않아 브루트포스보다 낮은 값을 내거나, 감당 불가능한 값을 낸다.

**만약 이 시점에 테스트가 통과한다면 멈추고 보고한다.** 우연히 통과하는 것일 수 있으므로, 절벽이 실제로 문제를 일으키는 프로필을 찾아야 한다. 통과했다면 `wealthy.cash`를 조정해 답이 15억 바로 근처에 오도록 맞춘 뒤 다시 확인한다.

- [ ] **Step 3: `buildSearchSegments`에 캡 경계를 더한다**

`src/lib/finance/affordable-price.ts`에서 `buildSearchSegments`를 찾아 이렇게 바꾼다:

```ts
/**
 * 탐색 범위를 절벽에서 나눈다.
 *
 * 절벽은 두 종류다.
 * 1. 정책대출 자격 상실(`eligibility.maxHousePrice`) — 그 가격을 넘으면
 *    상품 집합이 바뀌어 대출 한도가 불연속으로 움직인다.
 * 2. 절대캡 구간 경계(`absoluteCap.brackets[].upTo`) — 그 가격을 넘으면
 *    캡이 떨어진다.
 *
 * 두 경계 모두 **포함**이다. 즉 경계값 자체는 하위 구간에 속하므로
 * 다음 구간은 경계 + 1에서 시작한다.
 *
 * 룰셋에서 도출하며 값을 직접 하드코딩하지 않는다.
 */
function buildSearchSegments(rules: Rules): SearchSegment[] {
  const policyCliffs = rules.policyLoans
    .map((loan) => loan.eligibility.maxHousePrice)
    .filter((v): v is number => v !== undefined && v > 0);

  const capCliffs = rules.absoluteCap.brackets
    .map((bracket) => bracket.upTo)
    .filter((v): v is number => v !== null && v > 0);

  const boundaries = Array.from(new Set([...policyCliffs, ...capCliffs])).sort(
    (a, b) => a - b,
  );

  const segments: SearchSegment[] = [];
  let low = 0;
  for (const boundary of boundaries) {
    if (low > SEARCH_UPPER_BOUND) break;
    segments.push({ low, high: Math.min(boundary, SEARCH_UPPER_BOUND) });
    low = boundary + 1;
  }
  if (low <= SEARCH_UPPER_BOUND) {
    segments.push({ low, high: SEARCH_UPPER_BOUND });
  }
  return segments;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `npx vitest run src/lib/finance/affordable-price.test.ts`
Expected: PASS

- [ ] **Step 5: 전체 스위트와 타입체크를 돌린다**

Run: `npx vitest run && npx tsc --noEmit`
Expected: 전부 PASS

- [ ] **Step 6: 커밋한다**

```bash
git add src/lib/finance/affordable-price.ts src/lib/finance/affordable-price.test.ts
git commit -m "fix: 이분 탐색이 절대캡 구간 절벽을 가로지르지 않게 한다

buildSearchSegments가 정책대출 자격 절벽만 보고 구간을 나눠서, 캡이
떨어지는 15억·25억을 이분 탐색이 그대로 가로질렀다. 탐색 상한이 100억이라
두 경계 모두 범위 안이다.

브루트포스와 대조하는 테스트를 함께 넣어, 절벽 근처에서 엔진이 진짜
최대를 찾는지 고정한다."
```

---

## Task 3: `rules/2026-08.json`과 배선

현행 고시값을 담은 룰셋을 만들고, 화면이 그것을 읽게 한다.

**Files:**
- Create: `rules/2026-08.json`
- Create: `src/lib/finance/golden-2026-08.test.ts`
- Modify: `src/state/useAffordability.ts:2`
- Modify: `src/state/useAffordability.test.ts` (버전 문자열 한 줄)

**Interfaces:**
- Consumes: Task 1의 `absoluteCap` 스키마, Task 2의 탐색 구간

- [ ] **Step 1: `rules/2026-08.json`을 만든다**

`rules/2026-03.json`을 복사한 뒤 아래 값들을 바꾼다. **바뀌지 않는 값(ltv, brokerageFee, housingBond, acquisitionTax, legalFee, movingCost, dsrLimit, safetyThreshold, loanTermMonths, safetyStressSurcharge)은 그대로 둔다.**

```json
{
  "version": "2026-08",
  "effectiveFrom": "2026-08-01",
  "_scope": "수도권 기준. 10·15 대책의 스트레스 금리 하한 3%와 주택가격 구간별 대출한도는 둘 다 '수도권·규제지역' 적용이므로 이 파일의 범위와 정확히 일치한다 — stressDSR.surcharge가 스칼라로 남아 있는 것이 지금은 문제가 되지 않는 이유다. ltv는 규제지역/비규제지역 축을 이미 갖고 있다. 수도권/비수도권 축은 어디에도 없으므로, 다른 지역을 지원하려면 값 교체가 아니라 스키마에 축을 추가해야 한다.",
  "baseRate": 0.0453,
  "loanTermMonths": 360,
  "stressDSR": { "stage": 3, "surcharge": 0.03 },
  "safetyStressSurcharge": 0.02,
  "ltv": {
    "regulated":   { "default": 0.4, "firstTimeBuyer": 0.7 },
    "unregulated": { "default": 0.7, "firstTimeBuyer": 0.7 }
  },
  "brokerageVatRate": 0.1,
  "housingBond": {
    "_note": "제1종 국민주택채권. 시가표준액(공동주택 공시가격) 기준 매입 후 즉시 매도 시의 할인 손실이 실제 부담액이다. brackets의 매입률(perThousand)은 별표의 '특별시·광역시' 열 값이다. '그 밖의 지역'(예: 경기도 상당수 시·군) 열은 더 낮다(최상위 구간 1,000원당 31원 vs 26원). 이 파일은 특별시·광역시 값만 담으므로 그 밖의 지역 거주자에게는 실제보다 최대 약 20% 과대청구된다 — 과대평가는 안전한 방향이지만, 별표와 대조할 때는 이 열 선택을 알고 있어야 한다.",
    "assumedPriceToStandardRatio": 0.7,
    "assumedDiscountRate": 0.08,
    "brackets": [
      { "upTo": 20000000,   "perThousand": 0 },
      { "upTo": 50000000,   "perThousand": 13 },
      { "upTo": 100000000,  "perThousand": 19 },
      { "upTo": 160000000,  "perThousand": 21 },
      { "upTo": 260000000,  "perThousand": 23 },
      { "upTo": 600000000,  "perThousand": 26 },
      { "upTo": null,       "perThousand": 31 }
    ]
  },
  "absoluteCap": {
    "_note": "10·15 대책(2025-10-16 시행). 수도권·규제지역 주택구입 목적 주담대 한도를 주택가격 구간별로 나눈다. upTo는 포함(이하)이다 — 규제 원문이 '15억 원 이하'로 쓰기 때문이며, 이 파일의 다른 구간 배열(housingBond.brackets, brokerageFee)과 취득세는 배타(미만)라 여기만 다르다. 조회는 반드시 calcAbsoluteCap을 쓴다.",
    "brackets": [
      { "upTo": 1500000000, "amount": 600000000 },
      { "upTo": 2500000000, "amount": 400000000 },
      { "upTo": null,       "amount": 200000000 }
    ]
  },
  "dsrLimit": 0.4,
  "safetyThreshold": { "safe": 0.25, "caution": 0.35, "stressedDanger": 0.4 },
  "acquisitionTax": {
    "lowRate": 0.01,
    "highRate": 0.03,
    "lowerBound": 600000000,
    "upperBound": 900000000,
    "localEducationTaxRatio": 0.1,
    "ruralTaxRate": 0.002,
    "ruralTaxAreaThresholdSqm": 85,
    "firstTimeBuyerReliefCap": 2000000,
    "firstTimeBuyerReliefPriceCap": 1200000000
  },
  "brokerageFee": [
    { "upTo": 50000000, "rate": 0.006, "cap": 250000 },
    { "upTo": 200000000, "rate": 0.005, "cap": 800000 },
    { "upTo": 900000000, "rate": 0.004, "cap": null },
    { "upTo": 1200000000, "rate": 0.005, "cap": null },
    { "upTo": 1500000000, "rate": 0.006, "cap": null },
    { "upTo": null, "rate": 0.007, "cap": null }
  ],
  "legalFee": 600000,
  "movingCost": 1500000,
  "policyLoans": [
    {
      "_note": "디딤돌대출. 고시 금리 범위 2.45~3.55%의 상단을 쓴다 — 우대금리는 사람마다 달라 보장할 수 없고, 높은 금리는 한도를 낮춰 안전한 방향으로 틀린다. maxAmount는 일반 기준 2.5억이다. 생애최초 3억·신혼 4억 축은 아직 스키마에 없어 과소 계상되지만, 그 방향은 안전하다.",
      "id": "디딤돌",
      "eligibility": {
        "requiresNoHome": true,
        "maxAnnualIncome": 60000000,
        "maxHousePrice": 500000000,
        "maxAreaSqm": 85
      },
      "maxAmount": 250000000,
      "rate": 0.0355
    },
    {
      "_note": "보금자리론. 주금공 2026-08-01 공시의 u-보금자리론 30년 만기 금리 5.20%를 쓴다. 아낌e는 5.10%로 더 낮지만, 높은 쪽이 한도를 낮춰 안전한 방향으로 틀린다. loanTermMonths(360 = 30년)와 만기를 맞춘 값이다.",
      "id": "보금자리론",
      "eligibility": {
        "requiresNoHome": true,
        "maxAnnualIncome": 70000000,
        "maxHousePrice": 600000000
      },
      "maxAmount": 360000000,
      "rate": 0.052
    }
  ]
}
```

- [ ] **Step 2: 파싱되는지 확인한다**

`scripts/check-2026-08.ts`를 임시로 만든다:

```ts
import { readFileSync } from "node:fs";
import { parseRules } from "../src/lib/finance/rules";

const raw: unknown = JSON.parse(readFileSync("rules/2026-08.json", "utf8"));
const r = parseRules(raw);
console.log(
  "파싱 성공:", r.version,
  "| 스트레스", r.stressDSR.surcharge,
  "| baseRate", r.baseRate,
  "| 캡 구간 수", r.absoluteCap.brackets.length,
);
```

Run: `npx tsx scripts/check-2026-08.ts`

Expected: `파싱 성공: 2026-08 | 스트레스 0.03 | baseRate 0.0453 | 캡 구간 수 3`

에러가 나면 메시지가 가리키는 필드를 고친다. 확인이 끝나면 이 파일을 지운다:

```bash
rm scripts/check-2026-08.ts
```

**`node -e`나 `tsx -e`에 `require()`와 `import()`를 섞지 말 것** — 이 저장소는 ESM이라 `ERR_AMBIGUOUS_MODULE_SYNTAX`가 난다.

- [ ] **Step 3: 2026-08 골든 테스트를 쓴다**

`src/lib/finance/golden-2026-08.test.ts`를 새로 만든다.

**기대값을 실행 출력에서 베껴오지 말 것.** 아래 테스트는 전부 범위 또는 구조로 단언하도록 짜여 있어, 손 계산으로 검증 가능한 성질만 고정한다.

```ts
import { describe, expect, it } from "vitest";
import rawRules2026_03 from "../../../rules/2026-03.json";
import rawRules from "../../../rules/2026-08.json";
import { calcAbsoluteCap, calcMaxLoan } from "./loan-limit";
import { parseRules } from "./rules";
import type { BuyerProfile } from "./types";

const rules = parseRules(rawRules);
const rules2026_03 = parseRules(rawRules2026_03);

/**
 * 2026-08 룰셋의 공식 수치 대조.
 *
 * 실패하면 코드보다 rules/2026-08.json을 먼저 의심한다.
 *
 * 2026-03용 골든 테스트(golden.test.ts)는 그 시점의 발표를 재현하는 것이
 * 일이므로 그대로 둔다. 이 파일은 현행 고시를 재현한다.
 */
describe("골든 테스트 2026-08 — 현행 고시 대조", () => {
  const base: BuyerProfile = {
    status: "무주택",
    cash: 1_000_000_000,
    annualIncome: 100_000_000,
    existingDebtAnnualPayment: 0,
    isFirstTimeBuyer: false,
    exclusiveAreaSqm: 84,
    isRegulatedArea: false,
  };

  // 출처: 10·15 주택시장 안정화 대책(2025-10-15 발표, 10-16 시행).
  // "스트레스 금리 하한을 수도권·규제지역 내 주담대에 한해 3%로 상향 조정".
  it("스트레스 가산금리가 3%다", () => {
    expect(rules.stressDSR.surcharge).toBe(0.03);
  });

  // 스트레스 1.5%p에서 3.0%p로 오르면 DSR 심사금리가 오르고, 같은 소득으로
  // 감당할 수 있는 원금이 줄어든다. 방향만 고정한다 — 정확한 금액은
  // baseRate와 함께 움직이므로 범위로 잡는다.
  it("같은 소득에서 DSR 한도가 2026-03보다 작다", () => {
    const oldLimit = calcMaxLoan(base, rules2026_03, 2_000_000_000).breakdown.DSR;
    const newLimit = calcMaxLoan(base, rules, 2_000_000_000).breakdown.DSR;
    expect(newLimit).toBeLessThan(oldLimit);
  });

  // 출처: 10·15 대책 — "주택가격(시가) 15억 원 이하 → 6억 원 /
  // 15억 초과 25억 이하 → 4억 원 / 25억 초과 → 2억 원".
  it.each([
    [1_000_000_000, 600_000_000],
    [1_500_000_000, 600_000_000],
    [1_500_000_001, 400_000_000],
    [2_500_000_000, 400_000_000],
    [2_500_000_001, 200_000_000],
    [5_000_000_000, 200_000_000],
  ])("가격 %d원에서 절대캡이 %d원이다", (price, expected) => {
    expect(calcAbsoluteCap(rules, price)).toBe(expected);
  });

  // 소득을 극단적으로 높여 DSR을 무력화하고, 캡이 실제로 한도를 결정하는지
  // 본다. 30억 주택이므로 캡은 2억이다.
  it("30억 주택에서는 한도가 2억이고 CAP이 제약이다", () => {
    const result = calcMaxLoan(
      { ...base, annualIncome: 5_000_000_000, cash: 10_000_000_000 },
      rules,
      3_000_000_000,
    );
    expect(result.amount).toBe(200_000_000);
    expect(result.binding).toBe("CAP");
  });

  // 20억 주택 → 캡 4억. LTV 70% = 14억이므로 캡이 이긴다.
  it("20억 주택에서는 한도가 4억이고 CAP이 제약이다", () => {
    const result = calcMaxLoan(
      { ...base, annualIncome: 5_000_000_000, cash: 10_000_000_000 },
      rules,
      2_000_000_000,
    );
    expect(result.amount).toBe(400_000_000);
    expect(result.binding).toBe("CAP");
  });

  // 출처: 주금공 2026-08-01 공시, u-보금자리론 30년 만기 5.20%.
  it("보금자리론 금리가 5.20%다", () => {
    const bogeum = rules.policyLoans.find((l) => l.id === "보금자리론");
    expect(bogeum?.rate).toBe(0.052);
  });

  // 출처: 디딤돌대출 고시 범위 2.45~3.55%의 상단.
  it("디딤돌 금리가 3.55%다", () => {
    const didim = rules.policyLoans.find((l) => l.id === "디딤돌");
    expect(didim?.rate).toBe(0.0355);
  });

  // 출처: 한국은행 가중평균금리 2026-06, 예금은행 신규취급액 기준
  // 고정형 주담대 4.53%.
  it("시중금리 가정이 4.53%다", () => {
    expect(rules.baseRate).toBe(0.0453);
  });

  // 규제지역 LTV는 10·15 대책 이후에도 무주택자 40%, 생애최초 70%다.
  it("규제지역 LTV가 무주택 40% · 생애최초 70%다", () => {
    expect(rules.ltv.regulated.default).toBe(0.4);
    expect(rules.ltv.regulated.firstTimeBuyer).toBe(0.7);
  });
});
```

- [ ] **Step 4: 골든 테스트를 돌린다**

Run: `npx vitest run src/lib/finance/golden-2026-08.test.ts`
Expected: PASS

- [ ] **Step 5: 화면이 읽는 룰셋을 교체한다**

`src/state/useAffordability.ts` 2번째 줄:

```ts
import rawRules from "../../rules/2026-03.json";
```

이렇게 바꾼다:

```ts
import rawRules from "../../rules/2026-08.json";
```

- [ ] **Step 6: 버전 단언을 고친다**

`src/state/useAffordability.test.ts`에서 이 줄을 찾는다:

```ts
    expect(rules.version).toBe("2026-03");
```

이렇게 바꾼다:

```ts
    expect(rules.version).toBe("2026-08");
```

**이것이 이 계획에서 기존 테스트를 수정하는 유일한 지점이다.** 다른 테스트가 깨지면 고치지 말고 보고한다.

- [ ] **Step 7: 전체 스위트와 타입체크를 돌린다**

Run: `npx vitest run && npx tsc --noEmit`
Expected: 전부 PASS

`src/App.test.tsx`나 `src/integration.test.tsx`가 깨진다면, 화면 문구가 룰셋 버전에 의존하지 않는다는 기존 설계가 실제로는 지켜지지 않았다는 뜻이다. **기대값을 고치지 말고 무엇이 어떻게 깨졌는지 보고한다.**

- [ ] **Step 8: 화면을 눈으로 확인한다**

Run: `npm run dev`

브라우저에서 확인할 것:
- 부제에 `2026-08` 기준 룰셋 라벨이 뜬다
- 현금 2억 / 연소득 6천만원 / 생애최초 체크 / 규제지역 체크 상태에서 결과가 나온다
- 그 결과가 5.35억이 아니라 **4.7억대**다 (스트레스 3% 반영 확인)

확인 후 서버를 끈다.

- [ ] **Step 9: 커밋한다**

```bash
git add rules/2026-08.json src/lib/finance/golden-2026-08.test.ts src/state/useAffordability.ts src/state/useAffordability.test.ts
git commit -m "feat: 현행 고시를 반영한 2026-08 룰셋으로 교체한다

스트레스 금리 1.5%→3.0%(10·15 대책), 시중금리 4.2%→4.53%(한은
가중평균 26-06 고정형), 보금자리론 4.2%→5.20%(주금공 26-08-01 공시
u- 30년), 디딤돌 3.2%→3.55%(고시 범위 상단), 절대캡을 주택가격
구간별 6억/4억/2억으로.

넷 다 기존 값이 '실제보다 더 살 수 있다'고 말하는 방향이었다. DSR이
제약인 구매자 — 소득 대비 무리하는 쪽에 가까운 사람 — 에게만 오차가
났고, 규모는 5,800만~7,400만원이었다.

정책대출 금리는 고시 범위의 위쪽을 쓴다. 우대금리는 사람마다 달라
보장할 수 없고, 높은 금리는 한도를 낮춰 안전한 방향으로 틀린다.

rules/2026-03.json은 과거 계산 재현을 위해 남긴다."
```

---

## 완료 확인

전부 끝난 뒤:

```bash
npx vitest run && npx tsc --noEmit
```

- 전체 스위트 통과
- 타입체크 클린
- `rules/2026-03.json`을 쓰는 테스트가 **한 줄도 안 바뀐 채** 통과
- 수정된 기존 테스트는 `src/state/useAffordability.test.ts`의 버전 문자열 하나뿐
