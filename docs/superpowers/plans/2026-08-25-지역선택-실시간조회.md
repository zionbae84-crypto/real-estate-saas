# 예산 확정 → 지역 선택 → 실시간 실거래가 조회 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 예산을 확정한 사용자가 광역단체→자치구를 골라 그 지역의 국토부 실거래가를 Vercel 백엔드로 즉시 조회하고, 조회 결과 안에서 행정동까지 좁혀 볼 수 있게 한다.

**Architecture:** 기존 배치 파이프라인(`scripts/pipeline/*`)의 순수 함수(`fetchAllPages`·`normalizeAll`·`aggregate`·`toEmittedUnit`)를 그대로 재사용해 Vercel 서버리스 함수(`api/complexes.ts`) 하나가 요청마다 메모리에서만 조회·집계한다. 프론트엔드는 `src/no-network.test.ts`가 지키는 "네트워크 호출 전면 금지"를 `src/lib/regionQuery.ts` 파일 하나만 예외로 좁혀 이 API를 부른다.

**Tech Stack:** Vite + React 19 + TypeScript(strict), Vitest, Vercel 서버리스 함수(Node 런타임, `@vercel/node`).

## Global Constraints

- 시세는 범위(`minPrice`~`maxPrice`)와 거래 건수로만 표기한다. `medianPrice`·`changeRate*`를 화면·API 응답 어디에도 내지 않는다 — `toEmittedUnit()`을 반드시 거친다.
- `src/` 안 네트워크 호출은 `src/lib/regionQuery.ts` **한 파일만** 예외다. 그 외 모든 `src/` 파일은 지금처럼 `fetch`·`XMLHttpRequest` 등을 쓸 수 없다.
- `src/lib/regionQuery.ts`는 재무 필드 이름(`cash`, `annualIncome`, `existingDebtAnnualPayment`, `ownedHomeCount`, `isFirstTimeBuyer`, `isRegulatedArea`)을 요청을 만드는 코드에 등장시키지 않는다.
- `PUBLIC_DATA_API_KEY`(국토부 API 키)는 에러 메시지·로그 어디에도 그대로 노출되지 않는다 — `redactKey()`를 거친다.
- 국토부 API 실패는 항상 성공(200)과 다른 HTTP 상태코드로 응답한다. 빈 배열(200)과 실패를 절대 같은 응답 모양으로 섞지 않는다.
- 지역이 `api/_data/regulated-regions.json`에 없으면 `isRegulatedArea: null`을 응답한다. `null`을 받은 화면은 기존 규제지역 가정을 건드리지 않는다.
- 모든 새 순수 함수는 파일 I/O·네트워크 I/O와 분리해 인자로 의존성을 주입받는다(테스트 가능하게).

---

## Task 1: 법정동 코드 데이터 확보 + 축약 파이프라인

**Files:**
- Create: `data/sources/legal-dong-codes.csv`
- Create: `scripts/pipeline/region-codes.ts`
- Create: `scripts/pipeline/region-codes.test.ts`
- Create: `data/legal-dong-codes.json` (스크립트가 생성, 커밋 대상)
- Modify: `data/README.md`
- Modify: `package.json`

**Interfaces:**
- Produces: `RegionCode` 타입(`{ regionCode: string; sidoName: string; sigunguName: string }`), `buildRegionCodes(rows: RawLegalDongRow[]): RegionCode[]` — Task 6이 그대로 소비한다.

- [ ] **Step 1: 원본 CSV를 받아 컬럼을 추린다**

공공데이터포털 "국토교통부_전국 법정동"(https://www.data.go.kr/data/15063424/fileData.do)에서 CSV를 내려받는다. 원본 컬럼은 `법정동코드,시도명,시군구명,읍면동명,리명,순번,생성일자`(20,561행, 2026-07-29 기준)다.

`법정동코드,시도명,시군구명,읍면동명` 네 컬럼만 남기고 UTF-8(BOM 없음)·LF로 `data/sources/legal-dong-codes.csv`에 저장한다. 헤더 행 포함, 법정동코드 오름차순으로 정렬한다(다시 받아 추려도 같은 바이트가 나오게).

```bash
head -3 data/sources/legal-dong-codes.csv
# 법정동코드,시도명,시군구명,읍면동명
# 1100000000,서울특별시,,
# 1111000000,서울특별시,종로구,
```

- [ ] **Step 2: 축약 함수의 실패하는 테스트를 쓴다**

```typescript
// scripts/pipeline/region-codes.test.ts
import { describe, expect, it } from "vitest";
import { buildRegionCodes, type RawLegalDongRow } from "./region-codes";

describe("buildRegionCodes", () => {
  it("법정동코드 앞 5자리로 묶어 시군구 단위로 중복 제거한다", () => {
    const rows: RawLegalDongRow[] = [
      { code: "1111010100", sidoName: "서울특별시", sigunguName: "종로구", dongName: "청운동" },
      { code: "1111010200", sidoName: "서울특별시", sigunguName: "종로구", dongName: "신교동" },
      { code: "1111000000", sidoName: "서울특별시", sigunguName: "종로구", dongName: "" },
    ];
    const result = buildRegionCodes(rows);
    expect(result).toEqual([
      { regionCode: "11110", sidoName: "서울특별시", sigunguName: "종로구" },
    ]);
  });

  it("시군구명이 빈 문자열인 행(시/도 자체를 가리키는 행)은 뺀다", () => {
    const rows: RawLegalDongRow[] = [
      { code: "1100000000", sidoName: "서울특별시", sigunguName: "", dongName: "" },
      { code: "1111010100", sidoName: "서울특별시", sigunguName: "종로구", dongName: "청운동" },
    ];
    const result = buildRegionCodes(rows);
    expect(result).toEqual([
      { regionCode: "11110", sidoName: "서울특별시", sigunguName: "종로구" },
    ]);
  });

  it("결과는 regionCode 오름차순으로 정렬된다", () => {
    const rows: RawLegalDongRow[] = [
      { code: "1168010100", sidoName: "서울특별시", sigunguName: "강남구", dongName: "역삼동" },
      { code: "1111010100", sidoName: "서울특별시", sigunguName: "종로구", dongName: "청운동" },
    ];
    const result = buildRegionCodes(rows);
    expect(result.map((r) => r.regionCode)).toEqual(["11110", "11680"]);
  });
});
```

- [ ] **Step 3: 테스트를 실행해 실패를 확인한다**

Run: `npx vitest run scripts/pipeline/region-codes.test.ts`
Expected: FAIL — `region-codes.ts`를 찾을 수 없다는 에러

- [ ] **Step 4: 최소 구현을 쓴다**

```typescript
// scripts/pipeline/region-codes.ts
import { readFileSync, writeFileSync } from "node:fs";
import { dirname, join } from "node:path";
import { fileURLToPath, pathToFileURL } from "node:url";
import { DATA_DIR } from "./config";

const HERE = dirname(fileURLToPath(import.meta.url));
export const REGION_CODES_SOURCE_PATH = join(DATA_DIR, "sources", "legal-dong-codes.csv");
export const REGION_CODES_OUTPUT_PATH = join(DATA_DIR, "legal-dong-codes.json");

export interface RawLegalDongRow {
  code: string;
  sidoName: string;
  sigunguName: string;
  dongName: string;
}

export interface RegionCode {
  regionCode: string;
  sidoName: string;
  sigunguName: string;
}

/** CSV 한 줄을 RawLegalDongRow로 바꾼다. 헤더 행은 호출자가 건너뛴다. */
function parseLine(line: string): RawLegalDongRow {
  const [code = "", sidoName = "", sigunguName = "", dongName = ""] = line.split(",");
  return { code, sidoName, sigunguName, dongName };
}

export function parseCsv(content: string): RawLegalDongRow[] {
  const lines = content.split("\n").filter((l) => l.trim() !== "");
  return lines.slice(1).map(parseLine);
}

/**
 * 법정동코드 앞 5자리(시군구 단위)로 묶어 중복 제거한다.
 *
 * 시군구명이 빈 문자열인 행은 뺀다 — 시/도 전체를 가리키는 행(예:
 * "1100000000,서울특별시,,")이라 시군구 선택기에는 필요 없다.
 */
export function buildRegionCodes(rows: RawLegalDongRow[]): RegionCode[] {
  const byRegionCode = new Map<string, RegionCode>();
  for (const row of rows) {
    if (row.sigunguName === "") continue;
    const regionCode = row.code.slice(0, 5);
    if (!byRegionCode.has(regionCode)) {
      byRegionCode.set(regionCode, {
        regionCode,
        sidoName: row.sidoName,
        sigunguName: row.sigunguName,
      });
    }
  }
  return [...byRegionCode.values()].sort((a, b) => a.regionCode.localeCompare(b.regionCode));
}

export function run(
  sourcePath: string = REGION_CODES_SOURCE_PATH,
  outputPath: string = REGION_CODES_OUTPUT_PATH,
): void {
  const content = readFileSync(sourcePath, "utf8");
  const rows = parseCsv(content);
  const regionCodes = buildRegionCodes(rows);
  writeFileSync(outputPath, JSON.stringify(regionCodes, null, 2));
}

if (import.meta.url === pathToFileURL(process.argv[1] ?? "").href) {
  run();
}
```

- [ ] **Step 5: 테스트를 실행해 통과를 확인한다**

Run: `npx vitest run scripts/pipeline/region-codes.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 6: 실제 데이터로 실행해 산출물을 만든다**

```bash
npx tsx scripts/pipeline/region-codes.ts
wc -l data/legal-dong-codes.json
# 시군구 약 250여 개 규모의 JSON 배열이 생성됐는지 확인한다
```

- [ ] **Step 7: `data/README.md`에 새 출처를 기록한다**

기존 schools.csv·subway-stations.csv 표 바로 아래 행을 추가한다:

```markdown
| `data/sources/legal-dong-codes.csv` | 국토교통부 「전국 법정동」(공공데이터포털, `data.go.kr/data/15063424`, 20,561행) | 2026-07-29 | 법정동코드, 시도명, 시군구명, 읍면동명 |
```

- [ ] **Step 8: `package.json`에 실행 스크립트를 추가한다**

```json
"pipeline:region-codes": "tsx scripts/pipeline/region-codes.ts"
```

- [ ] **Step 9: 커밋**

```bash
git add data/sources/legal-dong-codes.csv data/legal-dong-codes.json \
  scripts/pipeline/region-codes.ts scripts/pipeline/region-codes.test.ts \
  data/README.md package.json
git commit -m "feat: 법정동 코드에서 시군구 단위 지역 목록을 만든다"
```

---

## Task 2: `fetch.ts`의 페이지네이션 로직을 재사용 가능하게 export한다

**Files:**
- Modify: `scripts/pipeline/fetch.ts`
- Test: `scripts/pipeline/fetch.test.ts`

**Interfaces:**
- Produces: `fetchAllPages(regionCode, yearMonth, key, wait): Promise<PageAccumulator>`, `PageAccumulator { trades: RawTrade[]; failures: number; cancelled: number; truncated: boolean }`, `Waiter = (ms: number) => Promise<void>`, `MONTHS_BACK: number`, `defaultWait: Waiter` — Task 3이 그대로 가져다 쓴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```typescript
// scripts/pipeline/fetch.test.ts 파일 맨 아래에 추가
import { fetchAllPages, MONTHS_BACK } from "./fetch";

describe("fetchAllPages (export 확인)", () => {
  it("모듈 바깥에서 호출할 수 있다", async () => {
    const fake = async () => JSON.stringify({
      response: {
        header: { resultCode: "000" },
        body: { totalCount: 0, items: "" },
      },
    });
    // fetchOne이 내부에서 fetch를 쓰므로 globalThis.fetch를 모의한다.
    const original = globalThis.fetch;
    globalThis.fetch = (async () =>
      new Response(await fake(), { status: 200 })) as typeof fetch;
    try {
      const result = await fetchAllPages("11680", "202601", "dummy-key", async () => {});
      expect(result.trades).toEqual([]);
    } finally {
      globalThis.fetch = original;
    }
  });

  it("MONTHS_BACK은 12다", () => {
    expect(MONTHS_BACK).toBe(12);
  });
});
```

- [ ] **Step 2: 테스트를 실행해 실패를 확인한다**

Run: `npx vitest run scripts/pipeline/fetch.test.ts`
Expected: FAIL — `"fetchAllPages"is not exported by "scripts/pipeline/fetch.ts"` 종류의 타입/임포트 에러

- [ ] **Step 3: export 키워드를 추가한다**

`scripts/pipeline/fetch.ts`에서 다섯 선언 앞에 `export`를 붙인다(동작은 한 글자도 바꾸지 않는다):

```typescript
// 기존: const MONTHS_BACK = 12;
export const MONTHS_BACK = 12;

// 기존: type Waiter = (ms: number) => Promise<void>;
export type Waiter = (ms: number) => Promise<void>;

// 기존: const defaultWait: Waiter = (ms) => ...
export const defaultWait: Waiter = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

// 기존: interface PageAccumulator { ... }
export interface PageAccumulator {
  trades: RawTrade[];
  failures: number;
  cancelled: number;
  truncated: boolean;
}

// 기존: async function fetchAllPages(...)
export async function fetchAllPages(
  regionCode: string,
  yearMonth: string,
  key: string,
  wait: Waiter,
): Promise<PageAccumulator> {
  // 본문은 그대로 둔다
```

- [ ] **Step 4: 테스트를 실행해 통과를 확인한다**

Run: `npx vitest run scripts/pipeline/fetch.test.ts`
Expected: PASS (기존 테스트 전부 + 새 테스트 2개)

- [ ] **Step 5: 타입체크**

Run: `npm run typecheck`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add scripts/pipeline/fetch.ts scripts/pipeline/fetch.test.ts
git commit -m "refactor: fetchAllPages 등을 재사용 가능하게 export한다"
```

---

## Task 3: 라이브 조회 오케스트레이션 함수

**Files:**
- Create: `scripts/pipeline/live.ts`
- Create: `scripts/pipeline/live.test.ts`

**Interfaces:**
- Consumes: `fetchAllPages`, `Waiter`, `defaultWait`, `MONTHS_BACK`, `buildTargets`(Task 2), `normalizeAll`(`./normalize`), `aggregate`, `ComplexUnit`(`./aggregate`), `toEmittedUnit`, `EmittedComplexUnit`(`./emit`), `ReportConfig`(`./types`)
- Produces: `fetchLiveComplexes(regionCode: string, dong: string | null, now: Date, key: string, config: ReportConfig, wait?: Waiter): Promise<EmittedComplexUnit[]>` — Task 5(API 핸들러)가 그대로 부른다.

- [ ] **Step 1: 실패하는 단위 테스트를 쓴다**

```typescript
// scripts/pipeline/live.test.ts
import { describe, expect, it, vi } from "vitest";
import { fetchLiveComplexes } from "./live";
import type { ReportConfig } from "./types";

const CONFIG: ReportConfig = {
  widePriceRangeMinRatio: 2.0,
  widePriceRangeMinTradeCount: 4,
  lowConfidenceMinTrades: 3,
  emptyRatioWarnThreshold: 0.2,
};

function tradeItem(over: Partial<Record<string, unknown>> = {}) {
  return {
    sggCd: "11680",
    aptSeq: "11680-1",
    umdNm: "역삼동",
    aptNm: "테스트아파트",
    buildYear: "2010",
    excluUseAr: "84.9",
    floor: "5",
    dealAmount: "100,000",
    dealYear: "2026",
    dealMonth: "1",
    dealDay: "1",
    ...over,
  };
}

function responseBody(items: ReturnType<typeof tradeItem>[]) {
  return JSON.stringify({
    response: {
      header: { resultCode: "000" },
      body: { totalCount: items.length, items: { item: items } },
    },
  });
}

describe("fetchLiveComplexes", () => {
  it("한 지역의 12개월치를 병렬로 모아 집계한다", async () => {
    const calls: string[] = [];
    globalThis.fetch = (async (url: string | URL) => {
      const u = new URL(url);
      calls.push(u.searchParams.get("DEAL_YMD") ?? "");
      // 첫 달만 거래 하나, 나머지는 0건.
      const items = u.searchParams.get("DEAL_YMD") === "202601" ? [tradeItem()] : [];
      return new Response(responseBody(items), { status: 200 });
    }) as typeof fetch;

    const units = await fetchLiveComplexes(
      "11680",
      null,
      new Date("2026-01-15"),
      "dummy-key",
      CONFIG,
      async () => {},
    );

    expect(calls).toHaveLength(12); // MONTHS_BACK
    expect(units).toHaveLength(1);
    expect(units[0]?.complexName).toBe("테스트아파트");
    expect(units[0]?.tradeCount).toBe(1);
  });

  it("medianPrice·changeRate*를 응답에서 뺀다", async () => {
    globalThis.fetch = (async () =>
      new Response(responseBody([tradeItem()]), { status: 200 })) as typeof fetch;

    const units = await fetchLiveComplexes(
      "11680",
      null,
      new Date("2026-01-15"),
      "dummy-key",
      CONFIG,
      async () => {},
    );

    expect(units[0]).not.toHaveProperty("medianPrice");
    expect(units[0]).not.toHaveProperty("changeRate3m");
    expect(units[0]).not.toHaveProperty("changeRate12m");
  });

  it("dong을 주면 그 법정동 거래만 남긴다", async () => {
    globalThis.fetch = (async () =>
      new Response(
        responseBody([tradeItem({ umdNm: "역삼동" }), tradeItem({ aptSeq: "11680-2", umdNm: "삼성동", aptNm: "다른아파트" })]),
        { status: 200 },
      )) as typeof fetch;

    const units = await fetchLiveComplexes(
      "11680",
      "삼성동",
      new Date("2026-01-15"),
      "dummy-key",
      CONFIG,
      async () => {},
    );

    expect(units).toHaveLength(1);
    expect(units[0]?.legalDongName).toBe("삼성동");
  });

  it("한 달이라도 실패하면 전체를 던진다 — 절반만 모은 데이터를 성공으로 두갑시키지 않는다", async () => {
    globalThis.fetch = (async () => new Response("", { status: 500 })) as typeof fetch;

    await expect(
      fetchLiveComplexes("11680", null, new Date("2026-01-15"), "dummy-key", CONFIG, async () => {}),
    ).rejects.toThrow();
  });
});
```

- [ ] **Step 2: 테스트를 실행해 실패를 확인한다**

Run: `npx vitest run scripts/pipeline/live.test.ts`
Expected: FAIL — `live.ts`를 찾을 수 없다

- [ ] **Step 3: 구현한다**

```typescript
// scripts/pipeline/live.ts
import { buildTargets, defaultWait, fetchAllPages, MONTHS_BACK, type Waiter } from "./fetch";
import { normalizeAll } from "./normalize";
import { aggregate } from "./aggregate";
import { toEmittedUnit, type EmittedComplexUnit } from "./emit";
import type { ReportConfig } from "./types";

/**
 * 지역 하나의 최근 12개월치를 라이브로 조회해 집계한다.
 *
 * 전부 메모리에서만 처리한다 — 디스크에 아무것도 쓰지 않는다. 12개월인
 * 이유는 `aggregate()`가 저신뢰 판정 등에 12개월 창을 쓰기 때문이다(화면에
 * 보이는 창은 6개월이지만, 6개월만 받으면 이 판정이 배치 파이프라인과
 * 달라진다). 지역이 하나뿐이라 12개월을 병렬로 호출해도 무리가 없다 —
 * 배치 파이프라인의 순차+쓰로틀은 지역이 여러 개일 때만 필요했다.
 *
 * 한 달이라도 `fetchAllPages`가 던지면 이 함수도 그대로 던진다 — 절반만
 * 모은 데이터를 성공으로 응답하지 않기 위해서다. 호출자(API 핸들러)가
 * 이걸 실패 상태코드로 옮긴다.
 */
export async function fetchLiveComplexes(
  regionCode: string,
  dong: string | null,
  now: Date,
  key: string,
  config: ReportConfig,
  wait: Waiter = defaultWait,
): Promise<EmittedComplexUnit[]> {
  const targets = buildTargets(now, MONTHS_BACK, [regionCode]);
  const pages = await Promise.all(
    targets.map((t) => fetchAllPages(t.regionCode, t.yearMonth, key, wait)),
  );
  const trades = pages.flatMap((p) => p.trades);
  const normalized = normalizeAll(trades);
  const units = aggregate(normalized, now, config);
  const filtered = dong === null ? units : units.filter((u) => u.legalDongName === dong);
  return filtered.map(toEmittedUnit);
}
```

- [ ] **Step 4: 테스트를 실행해 통과를 확인한다**

Run: `npx vitest run scripts/pipeline/live.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: 패리티 테스트를 추가한다 — 배치 경로와 결과가 같은지**

목적은 "언젠가 `live.ts`가 `normalizeAll`·`aggregate` 호출 인자를 실수로 바꿔도 잡는" 것이다. `fetchAllPages`가 내부에서 `parseResponse`로 HTTP 응답을 `RawTrade[]`로 바꾸므로, 같은 HTTP 응답 본문을 `parseResponse`에 **직접** 흘려 얻은 `RawTrade[]`를 배치 경로(`normalizeAll` → `aggregate` → `toEmittedUnit`을 테스트가 직접 호출)에 먹이고, `fetchLiveComplexes`(라이브 경로)가 **같은** HTTP 응답을 mock으로 받았을 때 낸 결과와 비교한다. 픽스처를 다른 파일에서 가져올 필요가 없다 — 이 파일 위쪽에 이미 정의한 `tradeItem`·`responseBody`를 그대로 쓴다.

```typescript
// scripts/pipeline/live.test.ts에 추가
import { normalizeAll } from "./normalize";
import { aggregate } from "./aggregate";
import { toEmittedUnit } from "./emit";
import { parseResponse } from "./parse-response";

describe("배치 파이프라인과의 패리티", () => {
  it("같은 HTTP 응답이면 라이브 경로와 배치 경로(normalizeAll→aggregate→toEmittedUnit 직접 호출)가 같은 결과를 낸다", async () => {
    const items = [tradeItem(), tradeItem({ aptSeq: "11680-2", aptNm: "다른아파트", umdNm: "삼성동" })];
    const body = responseBody(items);
    const now = new Date("2026-01-15");

    // 라이브 경로: fetchLiveComplexes가 매달 같은 응답을 받는다고 mock한다.
    globalThis.fetch = (async () => new Response(body, { status: 200 })) as typeof fetch;
    const liveUnits = await fetchLiveComplexes("11680", null, now, "dummy-key", CONFIG, async () => {});

    // 배치 경로: 같은 HTTP 응답 본문을 parseResponse로 직접 파싱해
    // normalizeAll → aggregate → toEmittedUnit을 순서대로 호출한다.
    // fetchLiveComplexes는 이 응답을 12번(월별) 받으므로, 배치 경로도
    // 같은 trades를 12번 이어붙여 맞춘다.
    const oneMonth = parseResponse(body).trades;
    const twelveMonths = Array.from({ length: 12 }, () => oneMonth).flat();
    const batchUnits = aggregate(normalizeAll(twelveMonths), now, CONFIG).map(toEmittedUnit);

    expect(liveUnits).toEqual(batchUnits);
    expect(liveUnits.length).toBe(2);
  });
});
```

**주의:** `fetchLiveComplexes`는 `buildTargets`가 만든 12개(월) 타깃 각각에 대해 `fetchAllPages`를 부르고, 위 mock은 매번 같은 `body`를 반환하므로 라이브 경로는 실제로 같은 두 건의 거래를 12번(달마다) 받는다 — 그래서 배치 경로도 `oneMonth`를 12번 이어붙여야 입력이 정확히 같아진다. 이 반복 이유를 아래 주석으로 함께 남긴다(위 코드에 이미 포함).

- [ ] **Step 6: 전체 테스트 + 타입체크**

Run: `npx vitest run scripts/pipeline/live.test.ts && npm run typecheck`
Expected: PASS, 에러 없음

- [ ] **Step 7: 커밋**

```bash
git add scripts/pipeline/live.ts scripts/pipeline/live.test.ts
git commit -m "feat: 지역 하나를 라이브로 조회·집계하는 함수를 만든다"
```

---

## Task 4: 네트워크 가드를 좁히고 `regionQuery.ts`를 만든다

**Files:**
- Modify: `src/no-network.test.ts`
- Modify: `src/data/complexes.ts`
- Create: `src/lib/regionQuery.ts`
- Create: `src/lib/regionQuery.test.ts`

**Interfaces:**
- Consumes: `ComplexUnit`, `narrowLandLeasehold`(신규 export, `src/data/complexes.ts`)
- Produces: `fetchRegionComplexes(regionCode: string, dong: string | null): Promise<{ units: ComplexUnit[]; isRegulatedArea: boolean | null }>` — Task 7(`useRegionComplexes`)이 그대로 부른다.

- [ ] **Step 1: `src/data/complexes.ts`에서 `narrowLandLeasehold`를 export한다**

```typescript
// 기존: function narrowLandLeasehold(...)
export function narrowLandLeasehold(value: string | null): "Y" | "N" | null {
  return value === "Y" || value === "N" ? value : null;
}
```

- [ ] **Step 2: `no-network.test.ts`에 실패하는 가드 테스트를 먼저 쓴다**

```typescript
// src/no-network.test.ts 에 추가
describe("regionQuery 예외", () => {
  const REGION_QUERY_PATH = "src/lib/regionQuery.ts";

  it("regionQuery.ts는 정확히 하나만 존재하고, 그 안에 재무 필드 이름이 없다", () => {
    const content = readFileSync(REGION_QUERY_PATH, "utf8");
    const FORBIDDEN_FIELD_NAMES = [
      "cash",
      "annualIncome",
      "existingDebtAnnualPayment",
      "ownedHomeCount",
      "isFirstTimeBuyer",
      "isRegulatedArea",
    ];
    // isRegulatedArea는 응답 "받는" 쪽 타입 선언에는 등장해도 된다 —
    // 여기서 막는 것은 "요청을 만드는 데 쓰였는가"이므로, 응답 타입
    // 선언까지 전부 막으면 이 파일 자체를 만들 수 없다. 그래서
    // isRegulatedArea는 이 목록에서 뺀다(요청 URL 생성부는 regionCode·
    // dong만 받는다는 사실은 아래 별도 테스트가 확인한다).
    const requestFields = FORBIDDEN_FIELD_NAMES.filter((f) => f !== "isRegulatedArea");
    for (const field of requestFields) {
      expect(content, `${field}가 regionQuery.ts에 등장하면 안 된다`).not.toContain(field);
    }
  });

  it("regionQuery.ts는 상대경로 /api/complexes 외 다른 곳으로 요청하지 않는다", () => {
    const content = readFileSync(REGION_QUERY_PATH, "utf8");
    expect(content).toContain('"/api/complexes');
    expect(content).not.toMatch(/https?:\/\//);
  });
});
```

이 테스트가 참조하는 `readFileSync`는 파일 상단에 이미 import돼 있다(같은 파일의 기존 코드).

- [ ] **Step 3: `no-network.test.ts`의 스캔 제외 목록에 `regionQuery.ts`를 추가한다**

`sourceFiles()` 함수를 수정해 `regionQuery.ts`도 스캔에서 뺀다(단, "정확히 하나만 존재"는 이름이 정확히 `regionQuery.ts`로 끝나는 파일을 세어 확인한다 — 기존 `no-network.test.ts`가 자기 자신에 대해 하는 것과 같은 패턴):

```typescript
function sourceFiles(dir: string): string[] {
  return readdirSync(dir).flatMap((entry) => {
    const path = join(dir, entry);
    if (statSync(path).isDirectory()) return sourceFiles(path);
    if (!/\.(ts|tsx|css|html)$/.test(entry)) return [];
    if (entry.endsWith("no-network.test.ts")) return [];
    if (entry === "regionQuery.ts") return []; // Task 4에서 추가한 유일한 fetch 예외
    return [path];
  });
}
```

기존 "no-network.test.ts만 스캔에서 빠지고" 테스트 바로 아래에 짝 테스트를 추가한다:

```typescript
it("regionQuery.ts만 fetch 예외이고, 정확히 하나만 존재한다", () => {
  const matches = findByNameSuffix("src", "regionQuery.ts");
  expect(matches).toEqual(["src/lib/regionQuery.ts"]);
});
```

- [ ] **Step 4: 테스트를 실행해 실패를 확인한다**

Run: `npx vitest run src/no-network.test.ts`
Expected: FAIL — `src/lib/regionQuery.ts`가 아직 없어 `readFileSync`가 던진다

- [ ] **Step 5: `regionQuery.ts`를 구현한다**

```typescript
// src/lib/regionQuery.ts
import { narrowLandLeasehold, type ComplexUnit } from "../data/complexes";

export interface RegionComplexesResult {
  units: ComplexUnit[];
  isRegulatedArea: boolean | null;
}

interface ApiUnit extends Omit<ComplexUnit, "landLeasehold"> {
  landLeasehold: string | null;
}

interface ApiResponse {
  units: ApiUnit[];
  isRegulatedArea: boolean | null;
}

interface ApiErrorBody {
  error?: string;
}

/**
 * 지역 실거래가를 백엔드에서 조회한다. 이 앱에서 `fetch`를 쓰는 유일한
 * 파일이다(`src/no-network.test.ts` 참고). regionCode·dong만 인자로
 * 받고, 재무 정보(현금·소득·기존부채 등)는 어디에도 등장하지 않는다.
 *
 * 실패하면 던진다 — 빈 배열을 반환하지 않는다. 호출자(`useRegionComplexes`)가
 * "조회 실패"와 "결과 0건"을 구분해야 하는데, 여기서 실패를 삼키면 그
 * 구분이 불가능해진다.
 */
export async function fetchRegionComplexes(
  regionCode: string,
  dong: string | null,
): Promise<RegionComplexesResult> {
  const params = new URLSearchParams({ regionCode });
  if (dong !== null) params.set("dong", dong);

  const res = await fetch(`/api/complexes?${params.toString()}`);

  if (!res.ok) {
    const body = (await res.json().catch(() => null)) as ApiErrorBody | null;
    throw new Error(body?.error ?? `조회 실패 (HTTP ${res.status})`);
  }

  const body = (await res.json()) as ApiResponse;
  return {
    units: body.units.map((u) => ({ ...u, landLeasehold: narrowLandLeasehold(u.landLeasehold) })),
    isRegulatedArea: body.isRegulatedArea,
  };
}
```

- [ ] **Step 6: `regionQuery.ts`의 동작 테스트를 쓴다**

```typescript
// src/lib/regionQuery.test.ts
import { describe, expect, it, vi } from "vitest";
import { fetchRegionComplexes } from "./regionQuery";

describe("fetchRegionComplexes", () => {
  it("성공하면 units와 isRegulatedArea를 반환한다", async () => {
    globalThis.fetch = vi.fn(async () =>
      new Response(
        JSON.stringify({
          units: [
            {
              complexKey: "11680-1",
              complexName: "테스트",
              regionCode: "11680",
              legalDongName: "역삼동",
              builtYear: 2010,
              areaBucket: 84,
              maxExclusiveAreaSqm: 84.9,
              landLeasehold: "N",
              tradeCount: 1,
              minPrice: 1_000_000_000,
              maxPrice: 1_000_000_000,
              minFloor: 5,
              maxFloor: 5,
              unknownFloorCount: 0,
              lowConfidence: true,
            },
          ],
          isRegulatedArea: true,
        }),
        { status: 200 },
      ),
    ) as typeof fetch;

    const result = await fetchRegionComplexes("11680", null);
    expect(result.units).toHaveLength(1);
    expect(result.isRegulatedArea).toBe(true);
  });

  it("HTTP 실패면 던진다 — 빈 배열을 반환하지 않는다", async () => {
    globalThis.fetch = vi.fn(async () =>
      new Response(JSON.stringify({ error: "국토부 API 오류" }), { status: 502 }),
    ) as typeof fetch;

    await expect(fetchRegionComplexes("11680", null)).rejects.toThrow("국토부 API 오류");
  });

  it("regionCode·dong을 쿼리 파라미터로 보낸다", async () => {
    let requestedUrl = "";
    globalThis.fetch = vi.fn(async (url: string | URL) => {
      requestedUrl = url.toString();
      return new Response(JSON.stringify({ units: [], isRegulatedArea: null }), { status: 200 });
    }) as typeof fetch;

    await fetchRegionComplexes("11680", "역삼동");
    expect(requestedUrl).toBe("/api/complexes?regionCode=11680&dong=%EC%97%AD%EC%82%BC%EB%8F%99");
  });
});
```

- [ ] **Step 7: 전체 테스트 실행**

Run: `npx vitest run src/no-network.test.ts src/lib/regionQuery.test.ts`
Expected: PASS (기존 no-network 테스트 전부 + 새 테스트 5개 + regionQuery 테스트 3개)

- [ ] **Step 8: 타입체크 + 커밋**

```bash
npm run typecheck
git add src/no-network.test.ts src/data/complexes.ts src/lib/regionQuery.ts src/lib/regionQuery.test.ts
git commit -m "feat: 네트워크 가드를 regionQuery.ts 하나만 남기고 좁힌다"
```

---

## Task 5: 규제지역 목록 파일 + Vercel API 핸들러

**Files:**
- Create: `api/_data/regulated-regions.json`
- Create: `api/_lib/handleComplexes.ts`
- Create: `api/_lib/handleComplexes.test.ts`
- Create: `api/complexes.ts`
- Modify: `package.json`
- Modify: `tsconfig.json`

**Interfaces:**
- Consumes: `fetchLiveComplexes`(Task 3), `redactKey`(`scripts/pipeline/fetch.ts`)
- Produces: `handleComplexesRequest(query, deps): Promise<{ status: number; body: unknown }>` — `api/complexes.ts`의 얇은 어댑터가 그대로 쓴다.

- [ ] **Step 1: 규제지역 목록 파일을 만든다**

```json
// api/_data/regulated-regions.json
{
  "regulated": ["11650", "11680", "11710"],
  "nonRegulated": []
}
```

- [ ] **Step 2: 핸들러 로직의 실패하는 테스트를 쓴다**

```typescript
// api/_lib/handleComplexes.test.ts
import { describe, expect, it, vi } from "vitest";
import { handleComplexesRequest } from "./handleComplexes";
import type { EmittedComplexUnit } from "../../scripts/pipeline/emit";

const UNIT: EmittedComplexUnit = {
  complexKey: "11680-1",
  complexName: "테스트",
  regionCode: "11680",
  legalDongName: "역삼동",
  builtYear: 2010,
  areaBucket: 84,
  maxExclusiveAreaSqm: 84.9,
  landLeasehold: "N",
  tradeCount: 1,
  minPrice: 1_000_000_000,
  maxPrice: 1_000_000_000,
  minFloor: 5,
  maxFloor: 5,
  unknownFloorCount: 0,
  lowConfidence: true,
};

const REGIONS = { regulated: ["11680"], nonRegulated: ["11110"] };

describe("handleComplexesRequest", () => {
  it("regionCode가 없으면 400을 반환한다", async () => {
    const result = await handleComplexesRequest(
      { regionCode: null, dong: null },
      { fetchLive: vi.fn(), key: "dummy", regions: REGIONS },
    );
    expect(result.status).toBe(400);
  });

  it("regionCode가 5자리 숫자가 아니면 400을 반환한다", async () => {
    const result = await handleComplexesRequest(
      { regionCode: "abc", dong: null },
      { fetchLive: vi.fn(), key: "dummy", regions: REGIONS },
    );
    expect(result.status).toBe(400);
  });

  it("성공하면 200과 units·isRegulatedArea(regulated 목록)를 반환한다", async () => {
    const fetchLive = vi.fn().mockResolvedValue([UNIT]);
    const result = await handleComplexesRequest(
      { regionCode: "11680", dong: null },
      { fetchLive, key: "dummy", regions: REGIONS },
    );
    expect(result.status).toBe(200);
    expect(result.body).toEqual({ units: [UNIT], isRegulatedArea: true });
  });

  it("nonRegulated 목록에 있으면 isRegulatedArea: false를 반환한다", async () => {
    const fetchLive = vi.fn().mockResolvedValue([]);
    const result = await handleComplexesRequest(
      { regionCode: "11110", dong: null },
      { fetchLive, key: "dummy", regions: REGIONS },
    );
    expect((result.body as { isRegulatedArea: unknown }).isRegulatedArea).toBe(false);
  });

  it("어느 목록에도 없으면 isRegulatedArea: null을 반환한다", async () => {
    const fetchLive = vi.fn().mockResolvedValue([]);
    const result = await handleComplexesRequest(
      { regionCode: "99999", dong: null },
      { fetchLive, key: "dummy", regions: REGIONS },
    );
    expect((result.body as { isRegulatedArea: unknown }).isRegulatedArea).toBeNull();
  });

  it("국토부 조회가 실패하면 502와 에러 메시지를 반환한다 — 200+빈배열이 아니다", async () => {
    const fetchLive = vi.fn().mockRejectedValue(new Error("HTTP 500"));
    const result = await handleComplexesRequest(
      { regionCode: "11680", dong: null },
      { fetchLive, key: "dummy", regions: REGIONS },
    );
    expect(result.status).toBe(502);
    expect(result.status).not.toBe(200);
  });

  it("에러 메시지에 키가 노출되면 redact한다", async () => {
    const fetchLive = vi.fn().mockRejectedValue(new Error("HTTP 403: key=secret-key-value"));
    const result = await handleComplexesRequest(
      { regionCode: "11680", dong: null },
      { fetchLive, key: "secret-key-value", regions: REGIONS },
    );
    expect(JSON.stringify(result.body)).not.toContain("secret-key-value");
  });
});
```

- [ ] **Step 3: 테스트를 실행해 실패를 확인한다**

Run: `npx vitest run api/_lib/handleComplexes.test.ts`
Expected: FAIL — 모듈 없음

- [ ] **Step 4: 구현한다**

```typescript
// api/_lib/handleComplexes.ts
import { redactKey } from "../../scripts/pipeline/fetch";
import type { EmittedComplexUnit } from "../../scripts/pipeline/emit";

export interface RegulatedRegions {
  regulated: string[];
  nonRegulated: string[];
}

export interface HandleComplexesDeps {
  fetchLive: (regionCode: string, dong: string | null) => Promise<EmittedComplexUnit[]>;
  key: string;
  regions: RegulatedRegions;
}

export interface HandleComplexesQuery {
  regionCode: string | null;
  dong: string | null;
}

export interface HandleComplexesResult {
  status: number;
  body: unknown;
}

const REGION_CODE_PATTERN = /^\d{5}$/;

function resolveIsRegulated(regionCode: string, regions: RegulatedRegions): boolean | null {
  if (regions.regulated.includes(regionCode)) return true;
  if (regions.nonRegulated.includes(regionCode)) return false;
  return null;
}

/**
 * `/api/complexes` 요청을 처리하는 순수 로직. Vercel의 req/res 타입과
 * 분리해 둬서 실제 HTTP 서버 없이 테스트할 수 있다.
 */
export async function handleComplexesRequest(
  query: HandleComplexesQuery,
  deps: HandleComplexesDeps,
): Promise<HandleComplexesResult> {
  const { regionCode, dong } = query;

  if (regionCode === null || !REGION_CODE_PATTERN.test(regionCode)) {
    return { status: 400, body: { error: "regionCode는 5자리 숫자여야 합니다" } };
  }

  try {
    const units = await deps.fetchLive(regionCode, dong);
    return {
      status: 200,
      body: { units, isRegulatedArea: resolveIsRegulated(regionCode, deps.regions) },
    };
  } catch (e) {
    const message = redactKey(e instanceof Error ? e.message : String(e), deps.key);
    return { status: 502, body: { error: message } };
  }
}
```

- [ ] **Step 5: 테스트 실행 → 통과 확인**

Run: `npx vitest run api/_lib/handleComplexes.test.ts`
Expected: PASS (7 tests)

- [ ] **Step 6: `@vercel/node`를 devDependency로 추가하고 Vercel 어댑터를 만든다**

```bash
npm install --save-dev @vercel/node
```

```typescript
// api/complexes.ts
import type { VercelRequest, VercelResponse } from "@vercel/node";
import { fetchLiveComplexes } from "../scripts/pipeline/live";
import reportConfig from "../scripts/pipeline/report-config.json";
import regulatedRegions from "./_data/regulated-regions.json";
import { handleComplexesRequest } from "./_lib/handleComplexes";
import type { ReportConfig } from "../scripts/pipeline/types";

export default async function handler(req: VercelRequest, res: VercelResponse) {
  const regionCode = typeof req.query.regionCode === "string" ? req.query.regionCode : null;
  const dong = typeof req.query.dong === "string" ? req.query.dong : null;
  const key = process.env.PUBLIC_DATA_API_KEY;

  if (!key) {
    res.status(500).json({ error: "서버에 PUBLIC_DATA_API_KEY가 설정되지 않았습니다" });
    return;
  }

  const result = await handleComplexesRequest(
    { regionCode, dong },
    {
      fetchLive: (rc, d) => fetchLiveComplexes(rc, d, new Date(), key, reportConfig as ReportConfig),
      key,
      regions: regulatedRegions,
    },
  );

  res.status(result.status).json(result.body);
}
```

- [ ] **Step 7: `tsconfig.json`에 `api`를 포함시킨다**

```json
{
  "compilerOptions": { /* 그대로 */ },
  "include": ["src", "scripts", "seed-design", "api"]
}
```

- [ ] **Step 8: 타입체크 + 커밋**

```bash
npm run typecheck
git add api/_data/regulated-regions.json api/_lib/handleComplexes.ts \
  api/_lib/handleComplexes.test.ts api/complexes.ts package.json package-lock.json tsconfig.json
git commit -m "feat: 지역 실거래가 조회 API와 규제지역 목록 관리 파일을 추가한다"
```

---

## Task 6: 지역 선택기용 데이터 래퍼

**Files:**
- Create: `src/data/regions.ts`
- Create: `src/data/regions.test.ts`

**Interfaces:**
- Consumes: `data/legal-dong-codes.json`(Task 1)
- Produces: `SIDO_NAMES: readonly string[]`, `sigunguBySido(sidoName: string): readonly { regionCode: string; sigunguName: string }[]`, `regionNameByCode(regionCode: string): string | null` — Task 8(`RegionSelect`)이 그대로 쓴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```typescript
// src/data/regions.test.ts
import { describe, expect, it } from "vitest";
import { regionNameByCode, sigunguBySido, SIDO_NAMES } from "./regions";

describe("regions", () => {
  it("SIDO_NAMES는 중복 없이 정렬된 시도 목록이다", () => {
    expect(SIDO_NAMES).toContain("서울특별시");
    expect(new Set(SIDO_NAMES).size).toBe(SIDO_NAMES.length);
  });

  it("sigunguBySido는 그 시도의 구만 반환한다", () => {
    const seoul = sigunguBySido("서울특별시");
    expect(seoul.some((r) => r.sigunguName === "강남구")).toBe(true);
    expect(seoul.every((r) => r.regionCode.startsWith("11"))).toBe(true);
  });

  it("모르는 시도명이면 빈 배열이다", () => {
    expect(sigunguBySido("없는시도")).toEqual([]);
  });

  it("regionNameByCode는 '시도 시군구' 형태로 합쳐 반환한다", () => {
    expect(regionNameByCode("11680")).toBe("서울특별시 강남구");
  });

  it("모르는 코드면 null이다", () => {
    expect(regionNameByCode("00000")).toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `npx vitest run src/data/regions.test.ts`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현한다**

```typescript
// src/data/regions.ts
import rawRegionCodes from "../../data/legal-dong-codes.json";

export interface RegionCode {
  regionCode: string;
  sidoName: string;
  sigunguName: string;
}

const REGION_CODES: readonly RegionCode[] = rawRegionCodes;

export const SIDO_NAMES: readonly string[] = [...new Set(REGION_CODES.map((r) => r.sidoName))];

export function sigunguBySido(
  sidoName: string,
): readonly { regionCode: string; sigunguName: string }[] {
  return REGION_CODES.filter((r) => r.sidoName === sidoName).map((r) => ({
    regionCode: r.regionCode,
    sigunguName: r.sigunguName,
  }));
}

export function regionNameByCode(regionCode: string): string | null {
  const found = REGION_CODES.find((r) => r.regionCode === regionCode);
  return found === undefined ? null : `${found.sidoName} ${found.sigunguName}`;
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `npx vitest run src/data/regions.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: 타입체크 + 커밋**

```bash
npm run typecheck
git add src/data/regions.ts src/data/regions.test.ts
git commit -m "feat: 전국 시도/시군구 선택기용 데이터 래퍼를 만든다"
```

---

## Task 7: `useRegionComplexes` 훅

**Files:**
- Create: `src/state/useRegionComplexes.ts`
- Create: `src/state/useRegionComplexes.test.ts`

**Interfaces:**
- Consumes: `fetchRegionComplexes`(Task 4)
- Produces: `useRegionComplexes(): { status: "idle" | "loading" | "error" | "success"; units: ComplexUnit[]; isRegulatedArea: boolean | null; error: string | null; query: (regionCode: string) => void; retry: () => void }` — Task 9(`App.tsx`)가 그대로 쓴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```typescript
// src/state/useRegionComplexes.test.ts
import { act, renderHook, waitFor } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import * as regionQuery from "../lib/regionQuery";
import { useRegionComplexes } from "./useRegionComplexes";

describe("useRegionComplexes", () => {
  it("초기 상태는 idle이다", () => {
    const { result } = renderHook(() => useRegionComplexes());
    expect(result.current.status).toBe("idle");
  });

  it("query를 부르면 loading을 거쳐 success가 된다", async () => {
    vi.spyOn(regionQuery, "fetchRegionComplexes").mockResolvedValue({
      units: [],
      isRegulatedArea: true,
    });
    const { result } = renderHook(() => useRegionComplexes());

    act(() => result.current.query("11680"));
    expect(result.current.status).toBe("loading");

    await waitFor(() => expect(result.current.status).toBe("success"));
    expect(result.current.isRegulatedArea).toBe(true);
  });

  it("실패하면 error 상태가 되고, units는 빈 배열로 폴백하지 않는다(undefined 유지)", async () => {
    vi.spyOn(regionQuery, "fetchRegionComplexes").mockRejectedValue(new Error("조회 실패"));
    const { result } = renderHook(() => useRegionComplexes());

    act(() => result.current.query("11680"));
    await waitFor(() => expect(result.current.status).toBe("error"));
    expect(result.current.error).toBe("조회 실패");
    expect(result.current.units).toEqual([]);
  });

  it("retry는 마지막으로 조회한 regionCode로 다시 부른다", async () => {
    const spy = vi
      .spyOn(regionQuery, "fetchRegionComplexes")
      .mockRejectedValueOnce(new Error("첫 실패"))
      .mockResolvedValueOnce({ units: [], isRegulatedArea: null });

    const { result } = renderHook(() => useRegionComplexes());
    act(() => result.current.query("11680"));
    await waitFor(() => expect(result.current.status).toBe("error"));

    act(() => result.current.retry());
    await waitFor(() => expect(result.current.status).toBe("success"));

    expect(spy).toHaveBeenCalledTimes(2);
    expect(spy).toHaveBeenNthCalledWith(2, "11680", null);
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `npx vitest run src/state/useRegionComplexes.test.ts`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현한다**

```typescript
// src/state/useRegionComplexes.ts
import { useCallback, useRef, useState } from "react";
import type { ComplexUnit } from "../data/complexes";
import { fetchRegionComplexes } from "../lib/regionQuery";

export type RegionComplexesStatus = "idle" | "loading" | "error" | "success";

export interface RegionComplexesState {
  status: RegionComplexesStatus;
  units: ComplexUnit[];
  isRegulatedArea: boolean | null;
  error: string | null;
  query: (regionCode: string) => void;
  retry: () => void;
}

/**
 * 지역 실거래가 조회 상태. "조회 실패"와 "조회 성공+0건"을 status로
 * 명확히 가른다 — units가 빈 배열인지만으로는 둘을 구분할 수 없다
 * (`regionQuery.ts`의 `fetchRegionComplexes`는 실패 시 던지므로, 여기서
 * 그 예외를 삼켜 status: "error"로 옮긴다).
 */
export function useRegionComplexes(): RegionComplexesState {
  const [status, setStatus] = useState<RegionComplexesStatus>("idle");
  const [units, setUnits] = useState<ComplexUnit[]>([]);
  const [isRegulatedArea, setIsRegulatedArea] = useState<boolean | null>(null);
  const [error, setError] = useState<string | null>(null);
  const lastRegionCode = useRef<string | null>(null);

  const run = useCallback((regionCode: string) => {
    lastRegionCode.current = regionCode;
    setStatus("loading");
    setError(null);
    fetchRegionComplexes(regionCode, null)
      .then((result) => {
        setUnits(result.units);
        setIsRegulatedArea(result.isRegulatedArea);
        setStatus("success");
      })
      .catch((e: unknown) => {
        setUnits([]);
        setError(e instanceof Error ? e.message : "알 수 없는 오류");
        setStatus("error");
      });
  }, []);

  const query = useCallback((regionCode: string) => run(regionCode), [run]);

  const retry = useCallback(() => {
    if (lastRegionCode.current !== null) run(lastRegionCode.current);
  }, [run]);

  return { status, units, isRegulatedArea, error, query, retry };
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `npx vitest run src/state/useRegionComplexes.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: 타입체크 + 커밋**

```bash
npm run typecheck
git add src/state/useRegionComplexes.ts src/state/useRegionComplexes.test.ts
git commit -m "feat: 지역 실거래가 조회 상태(loading/error/empty/success) 훅을 만든다"
```

---

## Task 8: `RegionSelect` 컴포넌트

**Files:**
- Create: `src/components/RegionSelect.tsx`
- Create: `src/components/RegionSelect.test.tsx`

**Interfaces:**
- Consumes: `SIDO_NAMES`, `sigunguBySido`(Task 6)
- Produces: `<RegionSelect onSelect={(regionCode: string) => void} />` — Task 9가 위자드 2단계에 배치한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```typescript
// src/components/RegionSelect.test.tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import { RegionSelect } from "./RegionSelect";

describe("RegionSelect", () => {
  it("시/도를 고르기 전에는 구 선택이 없다", () => {
    render(<RegionSelect onSelect={vi.fn()} />);
    expect(screen.queryByLabelText("자치구")).not.toBeInTheDocument();
  });

  it("시/도를 고르면 그 시/도의 구 목록이 나타난다", async () => {
    render(<RegionSelect onSelect={vi.fn()} />);
    await userEvent.selectOptions(screen.getByLabelText("광역단체"), "서울특별시");
    expect(screen.getByLabelText("자치구")).toBeInTheDocument();
    expect(screen.getByRole("option", { name: "강남구" })).toBeInTheDocument();
  });

  it("구까지 고르고 확인을 누르면 onSelect가 regionCode로 불린다", async () => {
    const onSelect = vi.fn();
    render(<RegionSelect onSelect={onSelect} />);
    await userEvent.selectOptions(screen.getByLabelText("광역단체"), "서울특별시");
    await userEvent.selectOptions(screen.getByLabelText("자치구"), "강남구");
    await userEvent.click(screen.getByRole("button", { name: "이 지역으로 조회하기" }));
    expect(onSelect).toHaveBeenCalledWith("11680");
  });

  it("시/도만 고르고 구를 안 고르면 확인 버튼이 비활성화된다", async () => {
    render(<RegionSelect onSelect={vi.fn()} />);
    await userEvent.selectOptions(screen.getByLabelText("광역단체"), "서울특별시");
    expect(screen.getByRole("button", { name: "이 지역으로 조회하기" })).toBeDisabled();
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `npx vitest run src/components/RegionSelect.test.tsx`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현한다**

```typescript
// src/components/RegionSelect.tsx
import { useState } from "react";
import { SIDO_NAMES, sigunguBySido } from "../data/regions";

export interface RegionSelectProps {
  onSelect: (regionCode: string) => void;
}

/**
 * 광역단체 → 자치구 2단 선택.
 *
 * 행정동은 여기서 고르지 않는다 — 국토부 API가 동 단위 파라미터를
 * 지원하지 않고, 조회 결과 안에서 걸러야 하기 때문이다(부모 스펙 §3의
 * 설계 메모 참고). 그래서 이 컴포넌트는 구까지만 확정하면 끝이고, 동
 * 좁히기는 결과를 받은 뒤 다른 컴포넌트가 담당한다.
 */
export function RegionSelect({ onSelect }: RegionSelectProps) {
  const [sido, setSido] = useState<string | null>(null);
  const [sigungu, setSigungu] = useState<string | null>(null);

  const sigunguOptions = sido === null ? [] : sigunguBySido(sido);
  const selectedRegionCode =
    sigungu === null ? null : sigunguOptions.find((s) => s.sigunguName === sigungu)?.regionCode ?? null;

  return (
    <section className="region-select" aria-label="지역 선택">
      <div className="field">
        <label htmlFor="sido-select">광역단체</label>
        <select
          id="sido-select"
          value={sido ?? ""}
          onChange={(e) => {
            setSido(e.target.value === "" ? null : e.target.value);
            setSigungu(null);
          }}
        >
          <option value="">고르세요</option>
          {SIDO_NAMES.map((name) => (
            <option key={name} value={name}>
              {name}
            </option>
          ))}
        </select>
      </div>

      {sido !== null && (
        <div className="field">
          <label htmlFor="sigungu-select">자치구</label>
          <select
            id="sigungu-select"
            value={sigungu ?? ""}
            onChange={(e) => setSigungu(e.target.value === "" ? null : e.target.value)}
          >
            <option value="">고르세요</option>
            {sigunguOptions.map((s) => (
              <option key={s.regionCode} value={s.sigunguName}>
                {s.sigunguName}
              </option>
            ))}
          </select>
        </div>
      )}

      <button
        type="button"
        disabled={selectedRegionCode === null}
        onClick={() => {
          if (selectedRegionCode !== null) onSelect(selectedRegionCode);
        }}
      >
        이 지역으로 조회하기
      </button>
    </section>
  );
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `npx vitest run src/components/RegionSelect.test.tsx`
Expected: PASS (4 tests)

- [ ] **Step 5: 타입체크 + 커밋**

```bash
npm run typecheck
git add src/components/RegionSelect.tsx src/components/RegionSelect.test.tsx
git commit -m "feat: 광역단체·자치구 2단 지역 선택 컴포넌트를 만든다"
```

---

## Task 9: `App.tsx` 통합 — 위자드 게이팅 + 배선 + 문구 + `RegionFilter` 제거

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/App.test.tsx`
- Delete: `src/components/RegionFilter.tsx`
- Delete: `src/components/RegionFilter.test.tsx`

**Interfaces:**
- Consumes: `useRegionComplexes`(Task 7), `RegionSelect`(Task 8), `regionNameByCode`(Task 6)

- [ ] **Step 1: `App.test.tsx`에 실패하는 위자드 게이팅 테스트를 쓴다**

```typescript
// src/App.test.tsx에 추가 (describe("App - 단지 상세(화면 4)") 블록 밖, 새 describe로)
describe("App - 지역 선택 위자드", () => {
  beforeEach(() => {
    window.localStorage.clear();
  });

  it("예산을 확정하기 전에는 지역 선택 단계가 보이지 않는다", () => {
    render(<App />);
    expect(screen.queryByRole("region", { name: "지역 선택" })).not.toBeInTheDocument();
  });

  it("현금·소득을 입력하면 지역 선택 단계가 나타난다", async () => {
    render(<App />);
    await userEvent.type(screen.getByLabelText("사용가능 현금 예산"), "150000");
    await userEvent.type(screen.getByLabelText("연 소득 (세전)"), "15000");
    await userEvent.click(screen.getByLabelText("무주택"));
    expect(screen.getByRole("region", { name: "지역 선택" })).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `npx vitest run src/App.test.tsx -t "지역 선택 위자드"`
Expected: FAIL — 지금은 예산 확정 여부와 무관하게 (구 버전) `RegionFilter`가 항상 보인다

- [ ] **Step 3: `App.tsx`를 수정한다**

`RegionFilter` import를 지우고 새 배선으로 바꾼다:

```typescript
// 지우기
import { useMemo, useState } from "react";
import { RegionFilter } from "./components/RegionFilter";
import { COMPLEX_UNITS, DATA_AS_OF, REGIONS, type ComplexUnit } from "./data/complexes";

// 바꾸기
import { useEffect, useMemo, useState } from "react";
import { DATA_AS_OF, type ComplexUnit } from "./data/complexes";
import { RegionSelect } from "./components/RegionSelect";
import { useRegionComplexes } from "./state/useRegionComplexes";
```

`const [regionCodes, setRegionCodes] = useState<string[]>([]);`와 `handleRegionChange` 함수 전체를 지우고 다음으로 바꾼다:

```typescript
const regionComplexes = useRegionComplexes();
const [selectedDong, setSelectedDong] = useState<string | null>(null);

/**
 * 지역을 고르면 규제지역 여부를 API 응답으로 정한다. `null`(모르는
 * 지역)이면 손대지 않는다 — 기존 가정(기본값: 규제지역)이 그대로 남는다.
 * 목록에 있는 지역이면(true/false) 폼 상태에 반영해 위쪽 실구매 가능
 * 가격도 같은 전제로 계산되게 한다(기존 handleRegionChange와 같은 이유).
 */
useEffect(() => {
  if (regionComplexes.status !== "success") return;
  if (regionComplexes.isRegulatedArea !== null) {
    setField("isRegulatedArea", regionComplexes.isRegulatedArea);
  }
}, [regionComplexes.status, regionComplexes.isRegulatedArea]);

function handleRegionSelect(regionCode: string) {
  setSelectedDong(null);
  setVisibleCount(10);
  regionComplexes.query(regionCode);
}

const dongOptions = useMemo(
  () => [...new Set(regionComplexes.units.map((u) => u.legalDongName))].sort(),
  [regionComplexes.units],
);

const dongFilteredUnits = useMemo(
  () =>
    selectedDong === null
      ? regionComplexes.units
      : regionComplexes.units.filter((u) => u.legalDongName === selectedDong),
  [regionComplexes.units, selectedDong],
);
```

`complexList`가 `COMPLEX_UNITS` 대신 `dongFilteredUnits`를 쓰게 바꾸고, `regionCodes` 인자를 뺀다(이미 지역 하나로 좁혀진 데이터이므로):

```typescript
const complexList = useMemo(
  () =>
    profile === null || purchaseType !== "실거주" || regionComplexes.status !== "success"
      ? null
      : buildComplexList({
          units: dongFilteredUnits,
          profile,
          rules,
          regionCodes: [],
        }),
  [profile, purchaseType, regionComplexes.status, dongFilteredUnits],
);
```

`App.tsx`에는 지금 다음 블록이 있다(대략 415~433번째 줄, `{detail !== null ? <ComplexDetail .../> : ( ... )}` 삼항의 `else` 가지):

```tsx
                <>
                  <RegionFilter
                    regions={REGIONS}
                    selected={regionCodes}
                    onChange={handleRegionChange}
                  />
                  {complexList !== null && (
                    <ComplexList
                      result={complexList}
                      dataAsOf={DATA_AS_OF}
                      hasRegionFilter={regionCodes.length > 0}
                      noRepaymentCapacity={
                        affordability.result.loanLimit.breakdown.DSR === 0
                      }
                      visibleCount={visibleCount}
                      onShowMore={() => setVisibleCount((n) => n + 10)}
                      onSelect={handleSelectUnit}
                    />
                  )}
                </>
```

이 블록 전체를 다음으로 바꾼다(`detail !== null ? <ComplexDetail/> : (...)` 삼항의 바깥 구조·`affordability.result.loanLimit.breakdown.DSR === 0`·`setVisibleCount((n) => n + 10)`은 그대로 유지한다):

```tsx
                <>
                  <RegionSelect onSelect={handleRegionSelect} />

                  {regionComplexes.status === "loading" && (
                    <p>지역 실거래가를 조회하고 있어요…</p>
                  )}

                  {regionComplexes.status === "error" && (
                    <div className="region-query-error">
                      <p>지금 실거래가를 불러오지 못했어요.</p>
                      <button type="button" onClick={regionComplexes.retry}>
                        다시 시도
                      </button>
                    </div>
                  )}

                  {regionComplexes.status === "success" && regionComplexes.units.length === 0 && (
                    <p className="region-empty">
                      이 지역엔 최근 6개월 실거래가 자체가 없어요. 다른 지역을
                      선택해 보세요.
                    </p>
                  )}

                  {regionComplexes.status === "success" && regionComplexes.units.length > 0 && (
                    <>
                      {dongOptions.length > 1 && (
                        <div className="field">
                          <label htmlFor="dong-narrow">행정동으로 좁히기</label>
                          <select
                            id="dong-narrow"
                            value={selectedDong ?? ""}
                            onChange={(e) =>
                              setSelectedDong(e.target.value === "" ? null : e.target.value)
                            }
                          >
                            <option value="">전체</option>
                            {dongOptions.map((d) => (
                              <option key={d} value={d}>
                                {d}
                              </option>
                            ))}
                          </select>
                        </div>
                      )}
                      {complexList !== null && (
                        <ComplexList
                          result={complexList}
                          dataAsOf={DATA_AS_OF}
                          hasRegionFilter={false}
                          noRepaymentCapacity={
                            affordability.result.loanLimit.breakdown.DSR === 0
                          }
                          visibleCount={visibleCount}
                          onShowMore={() => setVisibleCount((n) => n + 10)}
                          onSelect={handleSelectUnit}
                        />
                      )}
                    </>
                  )}
                </>
```

**왜 "지역 자체에 실거래가 0건"을 `ComplexList`의 `hasRegionFilter`/`emptyBecauseOfFilter`에 맡기지 않고 직접 갈랐는가.** `buildComplexList`(`src/lib/complex-list.ts:172`)의 `emptyBecauseOfFilter`는 `shown.length === 0 && units.some(affordable)`로 계산된다 — `shown`은 `regionCodes` 필터를 통과하고도 예산이 맞는 것들이다. 이 스텝처럼 `regionCodes: []`(필터 없음)를 넘기면 `inRegion()`이 모든 유닛에 참이 되어 `shown === units.filter(affordable)`와 같아지고, 그러면 `shown.length === 0`과 `units.some(affordable)`가 동시에 참일 수 없다 — **`emptyBecauseOfFilter`가 구조적으로 항상 `false`가 된다.** `hasRegionFilter`를 `true`로 고정해도 이 값이 `false`인 이상 "지역을 넓혀 보세요" 분기는 절대 뜨지 않고, 진짜 원인이 "이 지역엔 데이터가 없다"인 경우도 "예산이 부족해요"로 잘못 표시된다 — 이 앱이 가장 경계하는 "모르는 것과 확인한 것을 같은 문구로 보여주는" 오류다. 그래서 "지역 조회 결과 자체가 0건"은 `buildComplexList`에 넘기기 **전에** 직접 갈라 처리하고, `hasRegionFilter`는 `false`로 둔다 — 그 브랜치는 이 새 흐름에서 더 이상 쓰이지 않는다(지역을 이미 하나로 확정한 뒤라 "넓혀 보라"는 조언 자체가 성립하지 않는다). `units.length > 0`인데 예산이 안 맞는 경우는 `buildComplexList`의 기존 예산 기반 `EmptyMessage`(`noRepaymentCapacity`/기본 "예산 부족" 문구)가 그대로, 올바르게 처리한다.

부제 문구를 수정한다:

```tsx
<span className="subtitle-privacy-note">
  {" "}
  · 입력한 재무정보는 이 브라우저를 벗어나지 않아요. 지역 실거래가 조회에는
  고른 지역 코드만 서버로 전송돼요.
</span>
```

`· 수도권` 리터럴은 지역이 전국으로 넓어졌으므로 지운다(이 스펙 §9 "하지 않는 것"에 문구 정비는 범위 밖이라 적었지만, 최소한 더 이상 사실이 아닌 "수도권"이라는 단어는 남기지 않는다 — 완전한 문구 재정비는 후속 작업으로 남긴다).

- [ ] **Step 4: `RegionFilter`를 지운다**

```bash
rm src/components/RegionFilter.tsx src/components/RegionFilter.test.tsx
```

- [ ] **Step 5: 테스트 실행**

Run: `npx vitest run src/App.test.tsx -t "지역 선택 위자드"`
Expected: PASS (2 tests)

- [ ] **Step 6: 전체 스위트 실행 — 기존 `App.test.tsx`·`integration.test.tsx`·`purchase-type.test.tsx`가 옛 `RegionFilter` 배선(`"지역"` 라벨의 체크박스 등)에 의존하던 부분을 찾아 새 흐름으로 고친다**

Run: `npx vitest run`
Expected: 처음엔 여러 실패가 나온다. 각 실패를 확인해 다음 패턴으로 고친다:

- `screen.getByLabelText("지역")` 류의 옛 `RegionFilter` 쿼리 → 삭제하거나 `RegionSelect`의 새 라벨(`"광역단체"`, `"자치구"`)로 교체
- 단지 목록이 뜨는 것을 전제한 테스트는, 지역을 먼저 선택하는 단계(`userEvent.selectOptions` 두 번 + 확인 버튼 클릭)를 추가하고 `regionComplexes`를 모의(`vi.spyOn(regionQuery, "fetchRegionComplexes")`)해야 한다
- `App - 단지 상세(화면 4)` 블록의 `vi.mock("./data/complexes", ...)`는 `COMPLEX_UNITS`를 더 이상 쓰지 않으므로, 대신 `src/lib/regionQuery`의 `fetchRegionComplexes`를 모의해 `DETAIL_TEST_UNIT`을 반환하게 바꾼다

이 스텝은 기존 테스트 개수가 많아 구체적인 진단은 실제 실행 결과를 보고 하나씩 처리한다 — 각 실패마다 "옛 정적 데이터 배선을 전제했는가"를 확인하고, 그렇다면 위 패턴으로 `RegionSelect` 선택 + `fetchRegionComplexes` 모의로 바꾼다.

- [ ] **Step 7: 전체 스위트 통과 확인**

Run: `npx vitest run`
Expected: PASS, 실패 0건

- [ ] **Step 8: 타입체크**

Run: `npm run typecheck`
Expected: 에러 없음

- [ ] **Step 9: 커밋**

```bash
git add src/App.tsx src/App.test.tsx
git rm src/components/RegionFilter.tsx src/components/RegionFilter.test.tsx
git commit -m "feat: 예산 확정 후 지역 선택·실시간 조회 위자드를 App에 배선한다"
```

---

## Self-Review 메모 (계획 작성자용, 실행자는 참고만)

- **스펙 커버리지:** §3(위자드 흐름)=Task 9, §4(지역 데이터)=Task 1·6, §5(백엔드 API)·§5-1(규제지역)=Task 3·5, §6(시세 원칙 승계)=Task 3의 `toEmittedUnit` 재사용으로 자동 충족, §7(4상태 구분)=Task 7·9, §8(네트워크 가드)=Task 4, §10(테스트 전략) 8개 항목 전부 각 Task의 테스트로 매핑됨.
- **알려진 빈틈:** Task 9의 Step 6은 기존 테스트 스위트의 정확한 실패 목록을 실행 전에는 알 수 없어 "패턴으로 고친다"는 지침으로 남겼다 — 다른 Task와 달리 완전한 코드를 미리 줄 수 없는 유일한 지점이다. 실행자는 이 스텝에서 실제 실패 메시지를 보고 판단해야 한다.
- **타입 일관성:** `EmittedComplexUnit`(scripts/pipeline/emit.ts) ↔ `ComplexUnit`(src/data/complexes.ts)은 필드가 동일하고, `regionQuery.ts`가 그 경계에서 `landLeasehold`만 다시 좁힌다 — 두 타입을 섞어 쓰는 곳이 없는지 각 Task의 타입체크 스텝이 확인한다.
- **초안 작성 중 잡은 버그(수정 완료):** 초안은 Task 9에서 `hasRegionFilter`를 무조건 `true`로 고정하고 `buildComplexList`에 `regionCodes: []`를 넘기려 했다. `emptyBecauseOfFilter`(`src/lib/complex-list.ts:172`)는 `regionCodes`가 빈 배열이면 구조적으로 항상 `false`가 되므로, 이 조합은 "지역에 실거래가 자체가 없음"과 "예산이 부족함"을 구분하지 못하는 채로 조용히 잘못된 문구를 보여줄 뻔했다 — Task 9 최종본은 `regionComplexes.units.length === 0`을 `buildComplexList` 호출 전에 직접 갈라 처리하고 `hasRegionFilter={false}`로 바꿔 이 문제를 없앴다.
