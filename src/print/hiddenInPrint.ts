/**
 * 인쇄할 때 화면에서 지우는 요소들의 CSS 선택자 목록.
 *
 * **이 배열이 유일한 출처(source of truth)다.** `src/styles.css`의
 * `@media print` 블록은 정확히 이 선택자들에만 숨김 규칙(`display: none`)을
 * 건다 — `scripts/printCss.test.ts`가 두 쪽이 항상 일치하는지 검사한다.
 * CSS만 고치고 여기를 빼먹거나, 반대로 여기만 늘리고 CSS를 안 고치면
 * 그 테스트가 잡는다.
 *
 * 여기 오르는 것은 전부 "종이 위에서는 조작할 수 없는 상호작용 장치"다.
 * 값·문구(등급·경고·면책·가정·전제) 표시는 **절대 이 목록에 있으면
 * 안 된다** — {@link MUST_SURVIVE_PRINT_CLASSES}에 있는 클래스명은 여기
 * 선택자 어디에도 등장하면 안 되고, `printCss.test.ts`가 그 경계를
 * 지킨다.
 */
export const PRINT_HIDDEN_SELECTORS: readonly string[] = [
  // 화면 1(영상 위 입력) 전체. `position: fixed; inset: 0; overflow: hidden`에
  // 불투명한 `--ink` 바탕을 깐 레이어라, 인쇄에서 풀지 않으면 `phase`가
  // "입력"인 채로 인쇄할 때(Cmd+P는 어느 단계에서든 눌린다) 첫 장을 통째로
  // 덮어 `PrintSummary`·`BudgetResult`·경고·면책이 종이에 나오지 않는다.
  // design.md §6의 규칙("overflow/max-height 제약을 건 요소는 반드시
  // @media print에서 푼다")이 그대로 걸리는 자리다.
  //
  // **푸는 대신 통째로 지우는 이유**: 이 레이어가 담은 것 중 종이에
  // 남아야 하는 것이 없다. 입력 장치(`.profile-form`·`.region-select`)는
  // 이미 각각 이 목록에 있고, 부제의 룰셋 기준은 `PrintSummary`가 같은
  // `formatRuleVersionLabel(rules)`로 다시 적는다. 조회 로딩·실패 문구는
  // 종이에서 근거를 잃는 고아 문구다(`.complex-map-status`와 같은
  // 이유). 남은 것은 제목과 눈썹 라벨뿐이다.
  //
  // 단 하나 예외가 있었다: `.purchase-type-print`("구매 유형 — 실거주")는
  // MUST_SURVIVE_PRINT_CLASSES에 오른 보호 대상인데 유형 라디오 안에
  // 있어 이 레이어와 함께 사라졌다. 이 목록은 선택자 문자열만
  // 검사하므로(조상 관계는 보지 못한다) `printCss.test.ts`가 잡아 주지
  // 못하는 형태다 — 그래서 그 한 줄을 `App.tsx`가 이 레이어 **밖**에서
  // 그리도록 옮겼다. 라디오는 이제 없지만 그 줄은 그대로 밖에 남는다.
  ".entry-screen",
  // 프로필 입력 폼 전체(현금·연 소득·평형대 칩). 값 자체는 지우지
  // 않는다 — `PrintSummary`가 같은 값을 종이에 맞는 평문으로 별도로
  // 인쇄한다(App.tsx 참고). 입력란만 골라 숨기는
  // 대신 폼 전체를 숨기기로 한 이유: SEED TextField·Checkbox는 라벨·
  // 힌트·입력란이 한 덩어리로 묶여 있어 "입력란만" 골라내려면 SEED 내부
  // DOM 구조에 의존해야 하는데, 그 구조는 우리가 보장할 수 있는 계약이
  // 아니다(seed-design/ui/*는 벤더 스니펫이라 수정 금지). 폼 전체를
  // 지우고 값을 별도로 인쇄하면 이 의존을 아예 없앤다.
  ".profile-form",
  // 가격 슬라이더의 드래그 컨트롤(SEED Slider). 종이에서는 끌 수 없는
  // 장치다. 슬라이더가 가리키는 **값**(`.slider-price`)과 한계 경고
  // (`.slider-warning`)는 아래 위쪽 대출 배지가 바로 그 가격 기준으로
  // 계산되므로(전제) 지우지 않는다 — PriceSlider.tsx가 이 값들을 드래그
  // 컨트롤과 분리된 형제 요소로 둔 이유다.
  ".price-slider-control",
  // 광역단체·자치구 2단 지역 선택(과 조회 버튼). 부모 스펙이 인쇄에서
  // 지울 항목으로 명시했다. 선택한 지역이 남긴 흔적(규제지역 반영·목록에
  // 뜬 단지들의 법정동명)은 이미 다른 자리에 남아 있어 별도 텍스트로
  // 되살리지 않는다.
  //
  // 예전에는 `.region-filter`(3개 시군구 체크박스)였다. 지역이 전국으로
  // 넓어지며 컴포넌트가 `RegionSelect`로 바뀌었고, 숨겨야 하는 이유는
  // 그대로다 — 종이 위에서는 고를 수 없는 장치다.
  ".region-select",
  // 조회 결과를 행정동으로 좁히는 <select>. 같은 이유로 종이에서는
  // 조작할 수 없다. 실제로 무엇으로 좁혔는지는 목록에 뜬 행들의
  // 법정동명이 그대로 말한다.
  ".dong-narrow",
  // `.housing-type-select`(아래)와 `.dong-narrow`(위)를 함께 감싸는
  // 그룹이자, 그 아래 단지 목록과 갈라 주는 연한 구분선을 지닌 자리다
  // (App.tsx·styles.css 참고). 자식 둘은 이미 각자 이 목록에 있어
  // display:none이 걸리지만, 부모 자체는 지우지 않으면 자식이 사라진
  // 자리에 내용 없는 구분선(border-bottom)만 종이에 남는다 — 그래서
  // 부모도 통째로 지운다.
  ".complex-filters",
  // "조건 다시 넣기". 화면 1로 돌아가는 화면 전환 버튼이라 종이에서는
  // 누를 대상이 없다 — 지금까지 이 목록에 없어 죽은 버튼 모양이 종이에
  // 그대로 나왔다(Task 3이 이 버튼을 만들 때 함께 넣지 못한 것을
  // Task 4가 상단바로 옮기며 바로잡는다). 잃는 정보는 없다: 이 버튼은
  // 값도 문구도 아니고 조작 장치다.
  ".back-to-entry-button",
  // 목록 각 덩어리의 쪽 넘김(이전/다음). 종이에는 "쪽"이 없다 — 지금
  // 화면에 실제로 그려진 쪽의 행만 DOM에 있고(ComplexList.tsx가 그
  // 쪽만 자른다), 다른 쪽의 행은 애초에 로드돼 있지 않으므로 버튼을
  // 지워도 잃는 정보가 없다 — 누를 수도 없다. 예전 "N개 더 보기"
  // 버튼(`.complex-more`)과 같은 이유다.
  ".complex-pager",
  // 단지 상세에서 목록으로 돌아가는 버튼. 종이에는 "뒤로 갈" 목록 화면이
  // 없다.
  ".complex-detail-back",
  // 예산 상세 패널을 접는 버튼(BudgetPanel.tsx). 종이에는 접을 것이 없다 —
  // 패널은 화면에서만 여닫히고 인쇄에는 언제나 펼쳐진 채로 나간다
  // (그래야 그 안의 보호 대상 열 개가 종이에 남는다).
  //
  // **클래스 이름에 `budget-panel`을 쓰지 않았다.** `printCss.test.ts`의
  // 캐스케이드 검사(`couldCollide`)는 선택자 문자열의 **부분 일치**로
  // "같은 요소에 함께 걸릴 수 있는가"를 판단하는데, `.budget-panel-close`
  // 였다면 인쇄 블록의 해제 규칙 `.budget-panel { display: block }`이
  // 이 숨김을 이기는 것으로 **오탐**된다(실제로는 서로 다른 요소다).
  // 이름 하나로 그 오탐을 피한다.
  ".budget-detail-close",
  // PriceSlider 한계 경고 안의 "슬라이더를 내려 ~ 확인해 보세요" 조작
  // 지시. "이건 빌릴 수 있는 한계예요. 무리 없는 선은 따로 있어요"는
  // 이 인쇄물에서 가장 중요한 문장 중 하나라 반드시 남아야 하고, 뒤의
  // 조작 지시만 지운다.
  ".slider-action",
  // <details> summary 안의 "더 보기"/"모두 보기" 접미사, 그리고 상단바
  // 트리거("실구매 가능 가격")의 "자세히"/"닫기" 힌트. 인쇄에서는
  // <details>를 강제로 펼치므로(아래 @media print의 ::details-content
  // 규칙), 펼쳐진 내용 바로 위에서 "더 보기"라고 말하는 것은 이미 벌어진
  // 일을 하라고 시키는 죽은 지시문이 된다. 요약 문구 자체(예: "부대비용·
  // 정책대출·상세 설명", "네 가지 한도")는 펼쳐진 내용의 제목으로 여전히
  // 뜻이 있어 남긴다.
  //
  // 상단바 트리거도 같은 형태다(Task 5): 예산 상세 패널은 종이에서 늘
  // 펼쳐진 채로 나가므로, 그 값 옆의 "자세히"는 이미 벌어진 일을 하라고
  // 시키는 죽은 지시문이 된다. 라벨("실구매 가능 가격")과 값 자체는
  // 그 버튼 안에 그대로 남는다 — 종이의 첫 줄이다.
  ".fold-more-hint",
  // 호가 입력란. 종이에서는 채울 수 없다. 무엇을 넣었고 무엇이
  // 나왔는지는 `PriceCheck`의 결과가 호가와 근거(거래 건수·실거래
  // 범위·평형)를 다시 적으므로 종이에서 잃는 정보가 없다 —
  // `.price-verdict` 이하와, 입력란 **위**의 "적절한 값이 얼마인지
  // 매기지 않아요"(`.price-no-estimate`)는 아래 MUST_SURVIVE_PRINT_CLASSES가
  // 지킨다. 그 한 줄이 사라지면 종이를 건네받은 사람이 아래 판정을
  // "적정가 판정"으로 읽는다.
  ".price-check-form",
  // 단지 상세의 **매물가격 입력란**(ComplexDetail.tsx). 사용자 지시로 이
  // 화면의 가격 입력란을 하나로 합치면서 생겼다 — 위 `.price-check-form`과
  // 같은 이유로 종이에서는 채울 수 없다. 넣은 값 자체는 아래 결과들이
  // 각자 다시 적으므로(부대비용 합계·"최대 대출은 …"·호가 판정의
  // `.price-check-basis`) 종이에서 잃는 정보가 없다.
  //
  // ⚠ **이름에 `complex-detail`을 그대로 쓴다.** 아래
  // MUST_SURVIVE_PRINT_CLASSES에 `complex-detail`로 시작하는 보호 대상이
  // 있으면 이 목록은 부분 일치로 위반을 잡으므로(`printCss.test.ts`)
  // 그런 이름을 새로 만들지 않도록 주의해야 한다 — 지금 보호 대상은
  // `complex-detail-trades`·`complex-detail-facts`처럼 더 긴 이름이고,
  // 이 선택자(`.complex-detail-price-form`)는 그 어느 것의 부분
  // 문자열도 아니다.
  ".complex-detail-price-form",
  // "매달 나가는 돈" 계산기의 **입력란**(대출금액·금리·상환방식 라디오,
  // LoanCalculator.tsx). 바로 위 `.price-check-form`과 **같은 성격**이다 —
  // 종이 위에서는 채울 수도 고를 수도 없는 조작 장치다. 무엇을 넣었고
  // 무엇이 나왔는지는 결과 카드가 스스로 다시 적으므로(`.loan-calc-basis`가
  // "대출 1억 5,000만원 · 30년 · 연 4.53% · 원금균등으로 계산했어요"를,
  // `.loan-calc-note`가 이 숫자가 가정이라는 사실을 낸다) 종이에서 잃는
  // 정보가 없다.
  //
  // ⚠ **클래스 이름에 `loan-calc`를 쓰지 않았다.** 아래
  // MUST_SURVIVE_PRINT_CLASSES가 `loan-calc`를 보호 대상으로 올려 두는데,
  // 이 목록은 선택자 **문자열의 부분 일치**로 위반을 검사하므로
  // (`printCss.test.ts`) 이름이 `.loan-calc-form`이었다면 그 검사에
  // 오탐으로 걸린다. `.budget-detail-close`가 `.budget-panel`과의 충돌을
  // 피하려고 이름 하나로 같은 일을 한 전례를 그대로 따른다 — 그리고 그
  // 대가로 `loan-calc`로 시작하는 어떤 것도 실수로 이 숨김 목록에 오를
  // 수 없게 된다.
  ".loan-input-form",
  // 예산 상세의 "이 가격으로 샀을 때 최대로 빌린다면" 표 안, 적용 금리
  // **입력란**(PriceSlider.tsx). 위 `.loan-input-form`과 같은 이유다 —
  // 종이 위에서는 조작할 수 없는 장치다. 실제로 쓰인 금리는 이 입력란
  // 바로 위 `.hint`가 평문("지금은 연 4.53%로 계산했어요")으로 다시
  // 적으므로 종이에서 잃는 정보가 없다.
  ".price-slider-rate-input",
  // 갭투자·월세 수익형의 **값 입력란**(매매 예정가·보증금·현금·월세·
  // 운영비용·대출 답). 종이에서는 채울 수 없다. 무엇을 넣었고 무엇이
  // 나왔는지는 `PurchaseVerdict`가 결과 안에 값과 판정을 다시 적으므로
  // 종이에서 잃는 정보가 없다 — `.purchase-verdict` 이하와, 그 위의
  // `.purchase-loan-note`("한도는 계산하지 않아요")는 아래
  // MUST_SURVIVE_PRINT_CLASSES가 지킨다.
  ".purchase-form",
  // 단지 지도(네이버지도 SDK, ComplexMap.tsx). 인쇄는 정적 문서라 JS SDK가
  // 그리는 지도 타일·마커를 종이로 옮길 방법이 없다 — 높이만 있고 안이
  // 빈 400px 테두리 상자만 남는다. 지도가 가리키던 위치 정보는 이미
  // 목록 행의 법정동명·주소로 남아 있어 종이에서 잃는 정보가 없다.
  ".complex-map",
  // 지도 액자(ComplexMap.tsx). 위 `.complex-map`을 지워도 이 액자는 남아
  // 종이에 빈 테두리 상자가 된다 — 높이와 테두리만 있고 안이 빈 칸이다.
  //
  // 예전에는 액자 안의 **마커 색 범례**(`.complex-map-legend`)를 함께
  // 지우는 것도 이 선택자의 일이었다("이 색은 대출 없이 살 수 있다는
  // 뜻"이라는 안내는 지도가 없는 종이 위에서 가리킬 마커가 없는 고아
  // 문구다). 마커가 한 색이 되며 범례 자체가 사라져 그 이유는 없어졌지만,
  // 빈 액자를 지우는 이유는 그대로 남는다.
  ".complex-map-frame",
  // 지역을 바꾸는 동안 옛 지도를 그대로 보여주는 바깥 래퍼(App.tsx의
  // `mapDisplayData` 참고). 안의 `.complex-map-frame`은 위에서 이미
  // 지워지므로, 이 래퍼만 남으면 종이에 빈 상자가 남는다.
  ".complex-map-wrap",
  // "새 지역을 불러오는 중…" 배지 — 지역을 바꾸는 동안 옛 지도 위에
  // 뜬다. 종이는 정지된 한 순간이라 "불러오는 중"이라는 말 자체가
  // 성립하지 않는다 — 바로 위 `.complex-map-status`와 같은 이유다.
  ".complex-map-refresh-badge",
  // 좌표 조회(idle/loading/error)의 상태 문구를 감싸는 공용 래퍼(App.tsx).
  // 위에서 지도 자신을 이미 지웠으므로, 이 문구들을 인쇄에 남기면 근거를
  // 잃은 "지도를 불러오고 있어요…"나 눌러도 반응 없는 "다시 시도" 버튼만
  // 남는 고아 문구가 된다. 로딩·실패 두 상태를 하나의 래퍼로 감싸 한
  // 선택자로 지운다 — 성공 상태의 지도 자체는 위 `.complex-map`이 따로
  // 지운다.
  ".complex-map-status",
  // 지오코딩이 일부 주소에서 던졌을 때 지도 옆에 붙는 한 줄("일부 단지의
  // 위치를 확인하지 못했어요. 지도에 안 보이는 단지가 있을 수 있어요.").
  // 바로 위 `.complex-map-status`와 **같은 이유로** 지운다 — 지도가 없는
  // 종이 위에서 "지도에 안 보이는 단지가 있을 수 있어요"는 가리킬 대상이
  // 없는 고아 문구다. 여기서 잃는 정보도 없다: 이 문구는 지도에 못 그린
  // 단지가 있다는 말일 뿐이고, **목록은 좌표와 무관하게 그 단지들을
  // 전부 그대로 담아 종이에 나간다.**
  //
  // ComplexMap 자신이 내는 "지도에 표시할 단지의 위치를 확인하지
  // 못했어요"도 같은 클래스를 쓰지만, 그쪽은 `.complex-map` 안에 있어
  // 위에서 이미 함께 지워진다.
  ".complex-map-caveat",
  // 지도 칸에 그릴 단지가 없을 때의 문구("조건에 맞는 단지가 없어 지도에
  // 표시할 단지가 없어요.", App.tsx). 같은 이유다 — 지도가 없는 종이
  // 위에서는 지시 대상이 없다. 0건의 **진짜 원인**은 목록 칸 문구
  // (`.dong-empty` / `ComplexList`의 예산·상환능력 문구)가 말하고,
  // 그쪽은 인쇄에 그대로 남는다.
  ".complex-map-empty",
  // 매물 유형(아파트/오피스텔) 필터 자리 — 지금은 늘 "아파트"로 고정된
  // 비활성 select다(App.tsx 참고). 종이 위에서는 조작할 수 없는 장치이고,
  // 값 자체도 "아파트"뿐이라 지워도 잃는 정보가 없다.
  ".housing-type-select",
  // 한도 결정 내역 팝업의 말풍선 꼬리(`ComplexDetail.tsx`의
  // `.detail-binding-popup`). 화면에서는 이 삼각형이 팝업을 아이콘과
  // 시각적으로 이어 주지만, 종이에서는 팝업 자체가 흐름에 놓이는
  // 상자로 바뀌어(위 `.detail-binding-popup` 인쇄 규칙) 가리킬 아이콘이
  // 옆에 없다 — 뜻 없는 삼각형만 남는다.
  ".detail-binding-popup::before",
];

/**
 * 인쇄물에서 **절대 사라지면 안 되는** 요소들의 클래스 이름(선택자
 * 문법 없이, 순수 클래스명만).
 *
 * `printCss.test.ts`가 {@link PRINT_HIDDEN_SELECTORS}의 어떤 선택자에도
 * 이 이름들이 등장하지 않는지 검사한다. 나중에 누군가 숨김 범위를
 * 넓히다가(예: `.warning-list`를 실수로 추가) 등급·경고·면책·가정·전제가
 * 함께 사라지는 사고를 여기서 막는다.
 */
export const MUST_SURVIVE_PRINT_CLASSES: readonly string[] = [
  "safety-level", // 등급 글자(안전/주의/위험/확인 필요) — SafetyBadge
  "complex-level", // 목록 행의 등급 글자 — ComplexList
  "complex-burden", // 등급을 감싸는 행 요소 자체
  // 등급이 "안전"까지 가지 않고 멈춘 이유(토지임대부·모름). 종이에서
  // 사라지면 낯선 등급 글자만 남아, 종이를 건네받은 사람은 무엇이
  // 부족해서 멈춘 것인지 알 수 없다.
  "safety-grade-note", // 상세 배지 아래
  "complex-grade-note", // 목록 행의 등급 옆
  // "대출 없이 살 수 있어요" 옆의 단서. 이 단서가 빠지면 종이에는 이
  // 앱에서 가장 강한 안심 문구만 남는다.
  "complex-no-loan-caveat",
  // 토지임대부 표시. 종이에서 사라지면 안 되는 것 중에서도 무거운
  // 축이다 — 이 종이의 월 상환액에는 매달 나가는 토지 사용료가 들어
  // 있지 않고(우리 데이터에 금액이 없다), 그 사실이 빠지면 종이를
  // 건네받은 사람은 남은 숫자를 매달 나가는 돈 전부로 읽는다.
  // 모른다는 표시(`unknown`)도 같은 이유로 남아야 한다 — 빈 자리는
  // "토지임대부가 아니다"로 읽힌다.
  "land-lease-note", // 표시 전체(목록 행·상세·호가 세 화면 공용)
  "land-lease-badge", // 색이 아니라 글자로 존재하는 그 표시
  "safety-badge", // 등급 배지 섹션 전체
  "warning-list", // 엔진이 낸 경고
  "disclaimer", // 면책 문구(footer)
  // 예전에는 여기 "assumption-line"·"assumption-notice"(가정 문구
  // 목록, 구 `AssumptionLine`)가 있었다. 사용자 지시로 화면의 "계산
  // 전제" 덩어리를 통째로 삭제했다 — 같은 사실(기존 대출·규제지역
  // 판정 여부·전용면적 기준)은 `PrintSummary`(바로 아래, "print-summary")가
  // 이미 같은 원본에서 라벨·값 쌍으로 종이에 적고 있어, 문장 형태의
  // 별도 보호 대상을 남겨 둘 이유가 없다.
  "print-summary", // 전제(입력값)·룰셋 기준·인쇄일 요약
  "slider-price", // 슬라이더가 가리키는 가격(아래 표 계산의 전제)
  "slider-warning", // 그 가격이 한계라는 안내
  // "이 가격으로 샀을 때 최대로 빌린다면" 표 전체(등급·최대 대출
  // 가능 금액·월 상환액·부담률·적용 금리). 예전에는 이 정보가
  // `safety-badge`(위에서 보호) 하나였는데, PriceSlider와 합치며
  // 새 클래스로 감쌌다 — 이름을 바꿔도 보호가 끊기지 않게 새로 올린다.
  "price-slider-burden",
  // 예전에는 여기 "safe-line"(안전선, 구 `SafeLine`)이 있었다. 사용자
  // 지시로 헤드라인 카드에서 그 비교 줄을 뺐다 — 최대 가격과 같을 때가
  // 대부분이라 새 정보를 주지 못했다. 안전선 개념 자체는 `PriceSlider`의
  // 눈금 마커·`SafetyBadge`의 등급으로 남아 있다.
  "binding-explainer", // 무엇이 한도를 막았는지 설명
  "cost-breakdown", // 부대비용 내역
  /*
   * 단지 상세의 두 블록(design.md §6). 이 종이에서 사람이 가장 먼저
   * 보는 두 숫자가 여기 있다 — "취득시 부대비용"과 "매달 나가는 돈".
   * (화면에서 그 둘은 만원 단위로 반올림해 보인다 —
   * `formatWonRoundedToMan`. 표시만 반올림이고 아래 `cost-breakdown`
   * 내역과 `print-summary`는 그대로 정확한 원 단위라, 종이에도 두
   * 표기가 나란히 남는다.)
   *
   * `detail-stat-note`가 특히 무겁다: 그 줄이 **금리·기간 가정**을
   * 진다("대출 1억 2,744만원 · 30년 · 연 4.53% 가정")와 85㎡ 초과
   * 농어촌특별세 고지를 함께 낸다. 종이에서 그 줄만 사라지면 위의 월
   * 상환액이 어떤 금리·어떤 기간을 전제로 한 값인지가 통째로 없어지고,
   * 종이를 건네받은 사람은 그 금액을 확정된 값으로 읽는다 —
   * `assumption-notice`를 보호 대상으로 둔 것과 같은 이유다.
   */
  "detail-stat-value", // 두 블록의 값(또는 "대출 없이 살 수 있어요")
  "detail-stat-note", // 금리·기간 가정, 농어촌특별세 고지
  "detail-burden-ratio", // 소득 대비 부담률 한 줄
  /*
   * "매달 나가는 돈" 계산기(`LoanCalculator`). 사용자가 대출금액·금리를
   * 바꿔 둔 채로 인쇄하면 **그 가정 그대로** 종이에 남는다 — 가정을
   * 숨기지 않는다는 이 저장소의 규칙과 같은 방향이고, 그래서 결과 카드는
   * 값만이 아니라 무엇을 전제로 계산했는지(`.loan-calc-basis`)와 이
   * 숫자가 가정이라는 사실(`.loan-calc-note`), 계산하지 않았다면 그
   * 이유(`.loan-calc-guidance`·`.loan-calc-unavailable`)까지 함께 낸다.
   * 그 줄들이 사라지면 종이를 건네받은 사람은 표에 박힌 상환액을 은행이
   * 확정해 준 값으로 읽는다.
   *
   * **이름 하나로 그 전부를 지킨다.** 이 검사는 부분 일치라
   * `loan-calc` 한 줄이 `.loan-calc`(접기 자체)부터
   * `.loan-calc-result`·`-figures`·`-basis`·`-note`·`-guidance`·
   * `-unavailable`까지 전부 숨김 목록에 오르지 못하게 막는다. 입력란만
   * 다른 접두사(`.loan-input-form`)를 쓰는 이유가 정확히 이것이다(위
   * PRINT_HIDDEN_SELECTORS의 해당 항목 주석 참고).
   */
  "loan-calc",
  "policy-loan-list", // 정책대출 목록
  "no-budget", // 예산 0원 안내
  // 구매 유형별 재무 지표. 이 화면에서 가장 무거운 말은 "이 유형의
  // 대출 한도는 우리가 계산하지 않아요"다 — 종이에서 그것이 사라지면
  // 남은 지표들만 보고 한도가 문제되지 않는 것으로 읽는다.
  // 이 종이가 어떤 구매 유형을 전제하는가.
  //
  // **유형이 실거주 하나뿐이 된 뒤에도 남긴다.** 고를 수 없게 됐다고
  // 해서 전제가 사라진 것이 아니다 — 이 종이의 LTV·DSR·정책대출은
  // 전부 "내가 들어가 사는 집"을 전제로 고시된 값이고, 그 전제는
  // 종이 어디에도 다시 적히지 않는다. 이 줄을 빼면 그 전제가 다시
  // 말없이 깔린 상태로 돌아간다(구매 유형을 맨 앞에서 물었던 이유가
  // 정확히 "숨어 있던 전제를 밖으로 꺼내는 것"이었다). 화면에서는
  // 여전히 `display: none`이라 유형 하나짜리 라벨이 화면을 어지럽히지
  // 않는다.
  "purchase-type-print",
  "purchase-print-summary", // 입력값·인쇄일·어느 구매 유형 룰셋 기준인가
  "purchase-loan-note", // 한도를 계산하지 않는다는 사실과 그 이유
  "purchase-verdict", // 결과 영역 전체
  "purchase-overall", // 전체 결론 글자
  "purchase-metric", // 지표별 판정 줄
  "purchase-metric-verdict", // 그 줄의 등급 글자
  "purchase-metric-warning", // 보증금이 DSR에 안 잡힌다는 경고
  "purchase-stage", // 역전세 하락 단계별 필요 금액과 감당 여부
  "purchase-disclaimer", // 투자 자문이 아니라는 것과 전망하지 않는다는 것
  // 호가 위치 확인. 이 화면에서 가장 무거운 말은 판정이 아니라
  // **고지**다 — 층·향이 이 범위에 없다는 사실과 실거래 신고가 늦다는
  // 사실이 종이에서 사라지면, 남은 판정만 보고 우리가 틀린 확신을 준다.
  "price-no-estimate", // 이 화면이 값을 매기지 않는다는 사실
  "price-verdict", // 결과 영역 전체
  "price-overall", // 전체 결론 글자(멈춰 주세요 / 유보했어요 / …)
  "price-evidence", // 이 판단이 몇 건에 근거하는가(거래 건수·실거래 범위)
  "price-finding", // 줄별 판정
  "price-finding-verdict", // 그 줄의 등급 글자
  "price-budget-absent", // 예산 줄을 계산하지 않았다는 사실
  "price-disclosure", // 층·향 미반영·신고 지연·"바가지라는 뜻이 아니다"
  "price-disclaimer", // 감정평가가 아니라는 것과 전망하지 않는다는 것
  // 입지 사실. 이 영역에는 조작 장치가 하나도 없어 숨길 것이 없고,
  // 여기서 가장 무거운 말인 **고지**는 종이에서 더 중요하다 — 종이를
  // 건네받은 사람은 화면의 다른 맥락을 보지 못했다. 특히 학구도 고지가
  // 사라지면, 남은 학교 목록이 배정 결과처럼 읽힌다.
  "location-facts", // 영역 전체
  "location-state", // 지금이 "아직 위치를 몰라요"인지 아닌지의 글자
  "location-state-note", // 왜 그 상태인지(좌표를 못 구했다는 사실)
  "location-fact", // 역·학교 줄
  "location-fact-message", // 그 줄이 못 쟀으면 왜 못 쟀는지
  "location-school-list", // 반경 안의 학교들
  "location-distance", // 거리 값과 그 옆의 "직선거리" 라벨
  "location-disclosure", // 직선거리·학구도·소음 등 미반영·등급 아님
  "location-disclaimer", // 입지 평가가 아니라는 것과 반경이 법정 기준이 아니라는 것
];
